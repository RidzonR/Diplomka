# Vendor="MathWorks" Program="RoadRunner" Version="R2022b Update 4 (1.5.4.2b2a4e5a56e)" Date="2023-02-21T08:10:19"

import osc.standard

extend physical_object:
    def mw_point_euclidean_dist(reference: position_3d) -> length is undefined

scenario 22_test_scenario:

    map: map
    keep(map.map_file == "22testScenario.xodr")

    # Scenario Variables
    var sim_start_time: time = sample(environment.datetime, @root_phase.start)

    # Scenario Parameters
    environment: environment

    sedan: vehicle with:
        keep(it.color == white)
        keep(it.geometry_reference == "Vehicles/Sedan.fbx")
        keep(it.bounding_box.center.x == -5.96046e-08m)
        keep(it.bounding_box.center.y == -0.00706172m)
        keep(it.bounding_box.center.z == 0.822382m)
        keep(it.bounding_box.length == 4.55561m)
        keep(it.bounding_box.width == 1.93778m)
        keep(it.bounding_box.height == 1.264m)
        keep(it.center_of_gravity.x == -5.96046e-08m)
        keep(it.center_of_gravity.y == -0.00706172m)
        keep(it.center_of_gravity.z == 0.822382m)
        keep(it.vehicle_category == car)
        keep(it.axles.size == 2)
        keep(it.axles[1].max_steering == 0.698132rad)
        keep(it.axles[1].wheel_diameter == 0.688299m)
        keep(it.axles[1].track_width == 1.13434m)
        keep(it.axles[1].position_x == 1.38385m)
        keep(it.axles[1].position_z == 0.34415m)
        keep(it.axles[1].number_of_wheels == 2)
        keep(it.axles[2].max_steering == 0.698132rad)
        keep(it.axles[2].wheel_diameter == 0.688299m)
        keep(it.axles[2].track_width == 1.13434m)
        keep(it.axles[2].position_x == -1.32946m)
        keep(it.axles[2].position_z == 0.34415m)
        keep(it.axles[2].number_of_wheels == 2)
        keep(it.rear_overhang == 0.948339m)

    sedan_route_point_1: pose_3d with:
        keep(it.position.x == 173.779m)
        keep(it.position.y == 62.2973m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_2: pose_3d with:
        keep(it.position.x == 170.902m)
        keep(it.position.y == 60.2406m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_3: pose_3d with:
        keep(it.position.x == 164.842m)
        keep(it.position.y == 55.9076m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_4: pose_3d with:
        keep(it.position.x == 158.782m)
        keep(it.position.y == 51.5747m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_5: pose_3d with:
        keep(it.position.x == 152.722m)
        keep(it.position.y == 47.2417m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_6: pose_3d with:
        keep(it.position.x == 146.662m)
        keep(it.position.y == 42.9088m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_7: pose_3d with:
        keep(it.position.x == 140.601m)
        keep(it.position.y == 38.5758m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_8: pose_3d with:
        keep(it.position.x == 134.541m)
        keep(it.position.y == 34.2429m)
        keep(it.orientation.yaw == -2.52087rad)
    sedan_route_point_9: pose_3d with:
        keep(it.position.x == 128.481m)
        keep(it.position.y == 29.9099m)
        keep(it.orientation.yaw == -2.52103rad)
    sedan_route_point_10: pose_3d with:
        keep(it.position.x == 127.191m)
        keep(it.position.y == 28.9889m)
        keep(it.orientation.yaw == -2.52447rad)
    sedan_route_point_11: pose_3d with:
        keep(it.position.x == 125.903m)
        keep(it.position.y == 28.0804m)
        keep(it.orientation.yaw == -2.53257rad)
    sedan_route_point_12: pose_3d with:
        keep(it.position.x == 124.613m)
        keep(it.position.y == 27.1912m)
        keep(it.orientation.yaw == -2.54607rad)
    sedan_route_point_13: pose_3d with:
        keep(it.position.x == 123.317m)
        keep(it.position.y == 26.3283m)
        keep(it.orientation.yaw == -2.56498rad)
    sedan_route_point_14: pose_3d with:
        keep(it.position.x == 122.011m)
        keep(it.position.y == 25.4988m)
        keep(it.orientation.yaw == -2.58234rad)
    sedan_route_point_15: pose_3d with:
        keep(it.position.x == 121.353m)
        keep(it.position.y == 25.0987m)
        keep(it.orientation.yaw == -2.60286rad)
    sedan_route_point_16: pose_3d with:
        keep(it.position.x == 120.69m)
        keep(it.position.y == 24.7096m)
        keep(it.orientation.yaw == -2.6184rad)
    sedan_route_point_17: pose_3d with:
        keep(it.position.x == 120.024m)
        keep(it.position.y == 24.3325m)
        keep(it.orientation.yaw == -2.63529rad)
    sedan_route_point_18: pose_3d with:
        keep(it.position.x == 119.354m)
        keep(it.position.y == 23.9683m)
        keep(it.orientation.yaw == -2.65354rad)
    sedan_route_point_19: pose_3d with:
        keep(it.position.x == 118.678m)
        keep(it.position.y == 23.618m)
        keep(it.orientation.yaw == -2.67314rad)
    sedan_route_point_20: pose_3d with:
        keep(it.position.x == 117.998m)
        keep(it.position.y == 23.2825m)
        keep(it.orientation.yaw == -2.69937rad)
    sedan_route_point_21: pose_3d with:
        keep(it.position.x == 116.966m)
        keep(it.position.y == 22.8074m)
        keep(it.orientation.yaw == -2.72633rad)
    sedan_route_point_22: pose_3d with:
        keep(it.position.x == 115.92m)
        keep(it.position.y == 22.3661m)
        keep(it.orientation.yaw == -2.75877rad)
    sedan_route_point_23: pose_3d with:
        keep(it.position.x == 114.86m)
        keep(it.position.y == 21.959m)
        keep(it.orientation.yaw == -2.79121rad)
    sedan_route_point_24: pose_3d with:
        keep(it.position.x == 113.787m)
        keep(it.position.y == 21.5865m)
        keep(it.orientation.yaw == -2.82365rad)
    sedan_route_point_25: pose_3d with:
        keep(it.position.x == 112.702m)
        keep(it.position.y == 21.2489m)
        keep(it.orientation.yaw == -2.85609rad)
    sedan_route_point_26: pose_3d with:
        keep(it.position.x == 111.607m)
        keep(it.position.y == 20.9468m)
        keep(it.orientation.yaw == -2.88853rad)
    sedan_route_point_27: pose_3d with:
        keep(it.position.x == 110.503m)
        keep(it.position.y == 20.6803m)
        keep(it.orientation.yaw == -2.92097rad)
    sedan_route_point_28: pose_3d with:
        keep(it.position.x == 109.391m)
        keep(it.position.y == 20.4497m)
        keep(it.orientation.yaw == -2.95341rad)
    sedan_route_point_29: pose_3d with:
        keep(it.position.x == 108.272m)
        keep(it.position.y == 20.2553m)
        keep(it.orientation.yaw == -2.98585rad)
    sedan_route_point_30: pose_3d with:
        keep(it.position.x == 107.147m)
        keep(it.position.y == 20.0974m)
        keep(it.orientation.yaw == -3.01829rad)
    sedan_route_point_31: pose_3d with:
        keep(it.position.x == 106.018m)
        keep(it.position.y == 19.976m)
        keep(it.orientation.yaw == -3.05073rad)
    sedan_route_point_32: pose_3d with:
        keep(it.position.x == 104.885m)
        keep(it.position.y == 19.8913m)
        keep(it.orientation.yaw == -3.08317rad)
    sedan_route_point_33: pose_3d with:
        keep(it.position.x == 103.75m)
        keep(it.position.y == 19.8434m)
        keep(it.orientation.yaw == -3.11561rad)
    sedan_route_point_34: pose_3d with:
        keep(it.position.x == 102.615m)
        keep(it.position.y == 19.8323m)
        keep(it.orientation.yaw == 3.13513rad)
    sedan_route_point_35: pose_3d with:
        keep(it.position.x == 101.479m)
        keep(it.position.y == 19.858m)
        keep(it.orientation.yaw == 3.10269rad)
    sedan_route_point_36: pose_3d with:
        keep(it.position.x == 100.345m)
        keep(it.position.y == 19.9206m)
        keep(it.orientation.yaw == 3.07025rad)
    sedan_route_point_37: pose_3d with:
        keep(it.position.x == 99.2135m)
        keep(it.position.y == 20.0199m)
        keep(it.orientation.yaw == 3.03781rad)
    sedan_route_point_38: pose_3d with:
        keep(it.position.x == 98.0858m)
        keep(it.position.y == 20.1559m)
        keep(it.orientation.yaw == 3.00537rad)
    sedan_route_point_39: pose_3d with:
        keep(it.position.x == 96.9632m)
        keep(it.position.y == 20.3284m)
        keep(it.orientation.yaw == 2.97293rad)
    sedan_route_point_40: pose_3d with:
        keep(it.position.x == 95.8467m)
        keep(it.position.y == 20.5372m)
        keep(it.orientation.yaw == 2.94049rad)
    sedan_route_point_41: pose_3d with:
        keep(it.position.x == 94.7376m)
        keep(it.position.y == 20.7821m)
        keep(it.orientation.yaw == 2.90805rad)
    sedan_route_point_42: pose_3d with:
        keep(it.position.x == 93.637m)
        keep(it.position.y == 21.0628m)
        keep(it.orientation.yaw == 2.87561rad)
    sedan_route_point_43: pose_3d with:
        keep(it.position.x == 92.5461m)
        keep(it.position.y == 21.3791m)
        keep(it.orientation.yaw == 2.84317rad)
    sedan_route_point_44: pose_3d with:
        keep(it.position.x == 91.4661m)
        keep(it.position.y == 21.7306m)
        keep(it.orientation.yaw == 2.81073rad)
    sedan_route_point_45: pose_3d with:
        keep(it.position.x == 90.398m)
        keep(it.position.y == 22.117m)
        keep(it.orientation.yaw == 2.77829rad)
    sedan_route_point_46: pose_3d with:
        keep(it.position.x == 89.343m)
        keep(it.position.y == 22.5378m)
        keep(it.orientation.yaw == 2.74585rad)
    sedan_route_point_47: pose_3d with:
        keep(it.position.x == 88.3022m)
        keep(it.position.y == 22.9926m)
        keep(it.orientation.yaw == 2.71341rad)
    sedan_route_point_48: pose_3d with:
        keep(it.position.x == 87.2767m)
        keep(it.position.y == 23.4809m)
        keep(it.orientation.yaw == 2.68097rad)
    sedan_route_point_49: pose_3d with:
        keep(it.position.x == 86.2676m)
        keep(it.position.y == 24.0022m)
        keep(it.orientation.yaw == 2.64853rad)
    sedan_route_point_50: pose_3d with:
        keep(it.position.x == 85.2759m)
        keep(it.position.y == 24.5559m)
        keep(it.orientation.yaw == 2.61609rad)
    sedan_route_point_51: pose_3d with:
        keep(it.position.x == 84.3028m)
        keep(it.position.y == 25.1416m)
        keep(it.orientation.yaw == 2.58365rad)
    sedan_route_point_52: pose_3d with:
        keep(it.position.x == 83.3491m)
        keep(it.position.y == 25.7585m)
        keep(it.orientation.yaw == 2.55669rad)
    sedan_route_point_53: pose_3d with:
        keep(it.position.x == 82.7235m)
        keep(it.position.y == 26.1873m)
        keep(it.orientation.yaw == 2.53046rad)
    sedan_route_point_54: pose_3d with:
        keep(it.position.x == 82.1049m)
        keep(it.position.y == 26.6302m)
        keep(it.orientation.yaw == 2.51086rad)
    sedan_route_point_55: pose_3d with:
        keep(it.position.x == 81.4929m)
        keep(it.position.y == 27.086m)
        keep(it.orientation.yaw == 2.49262rad)
    sedan_route_point_56: pose_3d with:
        keep(it.position.x == 80.8871m)
        keep(it.position.y == 27.554m)
        keep(it.orientation.yaw == 2.47572rad)
    sedan_route_point_57: pose_3d with:
        keep(it.position.x == 80.2871m)
        keep(it.position.y == 28.0332m)
        keep(it.orientation.yaw == 2.46018rad)
    sedan_route_point_58: pose_3d with:
        keep(it.position.x == 79.6925m)
        keep(it.position.y == 28.5228m)
        keep(it.orientation.yaw == 2.43966rad)
    sedan_route_point_59: pose_3d with:
        keep(it.position.x == 78.5173m)
        keep(it.position.y == 29.5298m)
        keep(it.orientation.yaw == 2.4223rad)
    sedan_route_point_60: pose_3d with:
        keep(it.position.x == 77.3571m)
        keep(it.position.y == 30.5682m)
        keep(it.orientation.yaw == 2.40339rad)
    sedan_route_point_61: pose_3d with:
        keep(it.position.x == 76.2069m)
        keep(it.position.y == 31.6317m)
        keep(it.orientation.yaw == 2.38988rad)
    sedan_route_point_62: pose_3d with:
        keep(it.position.x == 75.0615m)
        keep(it.position.y == 32.7141m)
        keep(it.orientation.yaw == 2.38178rad)
    sedan_route_point_63: pose_3d with:
        keep(it.position.x == 73.915m)
        keep(it.position.y == 33.8093m)
        keep(it.orientation.yaw == 2.37833rad)
    sedan_route_point_64: pose_3d with:
        keep(it.position.x == 68.0496m)
        keep(it.position.y == 39.4222m)
        keep(it.orientation.yaw == 2.37819rad)
    sedan_route_point_65: pose_3d with:
        keep(it.position.x == 62.1841m)
        keep(it.position.y == 45.0352m)
        keep(it.orientation.yaw == 2.37819rad)
    sedan_route_point_66: pose_3d with:
        keep(it.position.x == 56.3186m)
        keep(it.position.y == 50.6482m)
        keep(it.orientation.yaw == 2.37819rad)
    sedan_route_point_67: pose_3d with:
        keep(it.position.x == 50.4531m)
        keep(it.position.y == 56.2612m)
        keep(it.orientation.yaw == 2.37808rad)
    sedan_route_point_68: pose_3d with:
        keep(it.position.x == 49.155m)
        keep(it.position.y == 57.505m)
        keep(it.orientation.yaw == 2.37525rad)
    sedan_route_point_69: pose_3d with:
        keep(it.position.x == 47.2184m)
        keep(it.position.y == 59.3749m)
        keep(it.orientation.yaw == 2.37098rad)
    sedan_route_point_70: pose_3d with:
        keep(it.position.x == 45.2919m)
        keep(it.position.y == 61.2554m)
        keep(it.orientation.yaw == 2.36672rad)
    sedan_route_point_71: pose_3d with:
        keep(it.position.x == 44.0103m)
        keep(it.position.y == 62.5162m)
        keep(it.orientation.yaw == 2.36394rad)
    sedan_route_point_72: pose_3d with:
        keep(it.position.x == 40.4123m)
        keep(it.position.y == 66.06m)
        keep(it.orientation.yaw == 2.36378rad)
    sedan_route_point_73: pose_3d with:
        keep(it.position.x == 36.8143m)
        keep(it.position.y == 69.6039m)
        keep(it.orientation.yaw == 2.36378rad)
    sedan_route_point_74: pose_3d with:
        keep(it.position.x == 33.2162m)
        keep(it.position.y == 73.1477m)
        keep(it.orientation.yaw == 2.36378rad)
    sedan_route_point_75: pose_3d with:
        keep(it.position.x == 29.6182m)
        keep(it.position.y == 76.6916m)
        keep(it.orientation.yaw == 2.36395rad)
    sedan_route_point_76: pose_3d with:
        keep(it.position.x == 28.8859m)
        keep(it.position.y == 77.4114m)
        keep(it.orientation.yaw == 2.36778rad)
    sedan_route_point_77: pose_3d with:
        keep(it.position.x == 28.1418m)
        keep(it.position.y == 78.1341m)
        keep(it.orientation.yaw == 2.37675rad)
    sedan_route_point_78: pose_3d with:
        keep(it.position.x == 27.3816m)
        keep(it.position.y == 78.8551m)
        keep(it.orientation.yaw == 2.39168rad)
    sedan_route_point_79: pose_3d with:
        keep(it.position.x == 26.6009m)
        keep(it.position.y == 79.5694m)
        keep(it.orientation.yaw == 2.41259rad)
    sedan_route_point_80: pose_3d with:
        keep(it.position.x == 25.7955m)
        keep(it.position.y == 80.2717m)
        keep(it.orientation.yaw == 2.43946rad)
    sedan_route_point_81: pose_3d with:
        keep(it.position.x == 24.9615m)
        keep(it.position.y == 80.9563m)
        keep(it.orientation.yaw == 2.46305rad)
    sedan_route_point_82: pose_3d with:
        keep(it.position.x == 24.5325m)
        keep(it.position.y == 81.29m)
        keep(it.orientation.yaw == 2.49012rad)
    sedan_route_point_83: pose_3d with:
        keep(it.position.x == 24.0951m)
        keep(it.position.y == 81.6169m)
        keep(it.orientation.yaw == 2.51027rad)
    sedan_route_point_84: pose_3d with:
        keep(it.position.x == 23.6489m)
        keep(it.position.y == 81.9361m)
        keep(it.orientation.yaw == 2.5319rad)
    sedan_route_point_85: pose_3d with:
        keep(it.position.x == 23.1934m)
        keep(it.position.y == 82.2467m)
        keep(it.orientation.yaw == 2.56083rad)
    sedan_route_point_86: pose_3d with:
        keep(it.position.x == 22.4951m)
        keep(it.position.y == 82.6932m)
        keep(it.orientation.yaw == 2.59055rad)
    sedan_route_point_87: pose_3d with:
        keep(it.position.x == 21.7813m)
        keep(it.position.y == 83.1145m)
        keep(it.orientation.yaw == 2.62636rad)
    sedan_route_point_88: pose_3d with:
        keep(it.position.x == 21.0529m)
        keep(it.position.y == 83.5099m)
        keep(it.orientation.yaw == 2.66217rad)
    sedan_route_point_89: pose_3d with:
        keep(it.position.x == 20.3107m)
        keep(it.position.y == 83.879m)
        keep(it.orientation.yaw == 2.69798rad)
    sedan_route_point_90: pose_3d with:
        keep(it.position.x == 19.5559m)
        keep(it.position.y == 84.2213m)
        keep(it.orientation.yaw == 2.73378rad)
    sedan_route_point_91: pose_3d with:
        keep(it.position.x == 18.7892m)
        keep(it.position.y == 84.5364m)
        keep(it.orientation.yaw == 2.76959rad)
    sedan_route_point_92: pose_3d with:
        keep(it.position.x == 18.0118m)
        keep(it.position.y == 84.8238m)
        keep(it.orientation.yaw == 2.8054rad)
    sedan_route_point_93: pose_3d with:
        keep(it.position.x == 17.2246m)
        keep(it.position.y == 85.0831m)
        keep(it.orientation.yaw == 2.84121rad)
    sedan_route_point_94: pose_3d with:
        keep(it.position.x == 16.4286m)
        keep(it.position.y == 85.3142m)
        keep(it.orientation.yaw == 2.87702rad)
    sedan_route_point_95: pose_3d with:
        keep(it.position.x == 15.6248m)
        keep(it.position.y == 85.5166m)
        keep(it.orientation.yaw == 2.91282rad)
    sedan_route_point_96: pose_3d with:
        keep(it.position.x == 14.8143m)
        keep(it.position.y == 85.69m)
        keep(it.orientation.yaw == 2.94863rad)
    sedan_route_point_97: pose_3d with:
        keep(it.position.x == 13.9981m)
        keep(it.position.y == 85.8344m)
        keep(it.orientation.yaw == 2.98444rad)
    sedan_route_point_98: pose_3d with:
        keep(it.position.x == 13.1773m)
        keep(it.position.y == 85.9494m)
        keep(it.orientation.yaw == 3.02025rad)
    sedan_route_point_99: pose_3d with:
        keep(it.position.x == 12.3529m)
        keep(it.position.y == 86.035m)
        keep(it.orientation.yaw == 3.05606rad)
    sedan_route_point_100: pose_3d with:
        keep(it.position.x == 11.5259m)
        keep(it.position.y == 86.091m)
        keep(it.orientation.yaw == 3.09187rad)
    sedan_route_point_101: pose_3d with:
        keep(it.position.x == 10.6975m)
        keep(it.position.y == 86.1174m)
        keep(it.orientation.yaw == 3.12767rad)
    sedan_route_point_102: pose_3d with:
        keep(it.position.x == 9.86862m)
        keep(it.position.y == 86.1141m)
        keep(it.orientation.yaw == -3.1197rad)
    sedan_route_point_103: pose_3d with:
        keep(it.position.x == 9.04042m)
        keep(it.position.y == 86.0811m)
        keep(it.orientation.yaw == -3.08389rad)
    sedan_route_point_104: pose_3d with:
        keep(it.position.x == 8.21393m)
        keep(it.position.y == 86.0185m)
        keep(it.orientation.yaw == -3.04809rad)
    sedan_route_point_105: pose_3d with:
        keep(it.position.x == 7.39022m)
        keep(it.position.y == 85.9264m)
        keep(it.orientation.yaw == -3.01228rad)
    sedan_route_point_106: pose_3d with:
        keep(it.position.x == 6.57033m)
        keep(it.position.y == 85.8048m)
        keep(it.orientation.yaw == -2.97647rad)
    sedan_route_point_107: pose_3d with:
        keep(it.position.x == 5.75532m)
        keep(it.position.y == 85.6539m)
        keep(it.orientation.yaw == -2.94066rad)
    sedan_route_point_108: pose_3d with:
        keep(it.position.x == 4.94623m)
        keep(it.position.y == 85.474m)
        keep(it.orientation.yaw == -2.90485rad)
    sedan_route_point_109: pose_3d with:
        keep(it.position.x == 4.14411m)
        keep(it.position.y == 85.2652m)
        keep(it.orientation.yaw == -2.86904rad)
    sedan_route_point_110: pose_3d with:
        keep(it.position.x == 3.34997m)
        keep(it.position.y == 85.0278m)
        keep(it.orientation.yaw == -2.83324rad)
    sedan_route_point_111: pose_3d with:
        keep(it.position.x == 2.56484m)
        keep(it.position.y == 84.7622m)
        keep(it.orientation.yaw == -2.79743rad)
    sedan_route_point_112: pose_3d with:
        keep(it.position.x == 1.78972m)
        keep(it.position.y == 84.4686m)
        keep(it.orientation.yaw == -2.76162rad)
    sedan_route_point_113: pose_3d with:
        keep(it.position.x == 1.02561m)
        keep(it.position.y == 84.1474m)
        keep(it.orientation.yaw == -2.72581rad)
    sedan_route_point_114: pose_3d with:
        keep(it.position.x == 0.273491m)
        keep(it.position.y == 83.7991m)
        keep(it.orientation.yaw == -2.69rad)
    sedan_route_point_115: pose_3d with:
        keep(it.position.x == -0.465679m)
        keep(it.position.y == 83.4241m)
        keep(it.orientation.yaw == -2.6542rad)
    sedan_route_point_116: pose_3d with:
        keep(it.position.x == -1.19095m)
        keep(it.position.y == 83.0229m)
        keep(it.orientation.yaw == -2.61839rad)
    sedan_route_point_117: pose_3d with:
        keep(it.position.x == -1.90139m)
        keep(it.position.y == 82.596m)
        keep(it.orientation.yaw == -2.58867rad)
    sedan_route_point_118: pose_3d with:
        keep(it.position.x == -2.36531m)
        keep(it.position.y == 82.2982m)
        keep(it.orientation.yaw == -2.55974rad)
    sedan_route_point_119: pose_3d with:
        keep(it.position.x == -2.82028m)
        keep(it.position.y == 81.9915m)
        keep(it.orientation.yaw == -2.5381rad)
    sedan_route_point_120: pose_3d with:
        keep(it.position.x == -3.26661m)
        keep(it.position.y == 81.6769m)
        keep(it.orientation.yaw == -2.51796rad)
    sedan_route_point_121: pose_3d with:
        keep(it.position.x == -3.70465m)
        keep(it.position.y == 81.3553m)
        keep(it.orientation.yaw == -2.49088rad)
    sedan_route_point_122: pose_3d with:
        keep(it.position.x == -4.55744m)
        keep(it.position.y == 80.6942m)
        keep(it.orientation.yaw == -2.46729rad)
    sedan_route_point_123: pose_3d with:
        keep(it.position.x == -5.38208m)
        keep(it.position.y == 80.0146m)
        keep(it.orientation.yaw == -2.44042rad)
    sedan_route_point_124: pose_3d with:
        keep(it.position.x == -6.18236m)
        keep(it.position.y == 79.3223m)
        keep(it.orientation.yaw == -2.41951rad)
    sedan_route_point_125: pose_3d with:
        keep(it.position.x == -6.96235m)
        keep(it.position.y == 78.6227m)
        keep(it.orientation.yaw == -2.40457rad)
    sedan_route_point_126: pose_3d with:
        keep(it.position.x == -7.72625m)
        keep(it.position.y == 77.921m)
        keep(it.orientation.yaw == -2.3956rad)
    sedan_route_point_127: pose_3d with:
        keep(it.position.x == -8.47829m)
        keep(it.position.y == 77.2218m)
        keep(it.orientation.yaw == -2.39176rad)
    sedan_route_point_128: pose_3d with:
        keep(it.position.x == -12.4544m)
        keep(it.position.y == 73.5177m)
        keep(it.orientation.yaw == -2.3916rad)
    sedan_route_point_129: pose_3d with:
        keep(it.position.x == -16.4306m)
        keep(it.position.y == 69.8137m)
        keep(it.orientation.yaw == -2.3916rad)
    sedan_route_point_130: pose_3d with:
        keep(it.position.x == -20.4067m)
        keep(it.position.y == 66.1096m)
        keep(it.orientation.yaw == -2.3916rad)
    sedan_route_point_131: pose_3d with:
        keep(it.position.x == -24.3828m)
        keep(it.position.y == 62.4055m)
        keep(it.orientation.yaw == -2.3916rad)
    sedan_route_point_132: pose_3d with:
        keep(it.position.x == -30.2236m)
        keep(it.position.y == 56.9644m)
        keep(it.orientation.yaw == -2.3916rad)
    sedan_route_point_133: pose_3d with:
        keep(it.position.x == -36.0644m)
        keep(it.position.y == 51.5232m)
        keep(it.orientation.yaw == -2.39175rad)
    sedan_route_point_134: pose_3d with:
        keep(it.position.x == -36.8262m)
        keep(it.position.y == 50.8154m)
        keep(it.orientation.yaw == -2.39677rad)
    sedan_route_point_135: pose_3d with:
        keep(it.position.x == -37.5835m)
        keep(it.position.y == 50.1226m)
        keep(it.orientation.yaw == -2.40843rad)
    sedan_route_point_136: pose_3d with:
        keep(it.position.x == -38.3412m)
        keep(it.position.y == 49.4509m)
        keep(it.orientation.yaw == -2.42789rad)
    sedan_route_point_137: pose_3d with:
        keep(it.position.x == -39.1041m)
        keep(it.position.y == 48.8058m)
        keep(it.orientation.yaw == -2.45514rad)
    sedan_route_point_138: pose_3d with:
        keep(it.position.x == -39.8759m)
        keep(it.position.y == 48.1934m)
        keep(it.orientation.yaw == -2.48021rad)
    sedan_route_point_139: pose_3d with:
        keep(it.position.x == -40.2663m)
        keep(it.position.y == 47.9013m)
        keep(it.orientation.yaw == -2.50982rad)
    sedan_route_point_140: pose_3d with:
        keep(it.position.x == -40.66m)
        keep(it.position.y == 47.6196m)
        keep(it.orientation.yaw == -2.53224rad)
    sedan_route_point_141: pose_3d with:
        keep(it.position.x == -41.0574m)
        keep(it.position.y == 47.3491m)
        keep(it.orientation.yaw == -2.5566rad)
    sedan_route_point_142: pose_3d with:
        keep(it.position.x == -41.4587m)
        keep(it.position.y == 47.0906m)
        keep(it.orientation.yaw == -2.58291rad)
    sedan_route_point_143: pose_3d with:
        keep(it.position.x == -41.8639m)
        keep(it.position.y == 46.8449m)
        keep(it.orientation.yaw == -2.61118rad)
    sedan_route_point_144: pose_3d with:
        keep(it.position.x == -42.2732m)
        keep(it.position.y == 46.6129m)
        keep(it.orientation.yaw == -2.64903rad)
    sedan_route_point_145: pose_3d with:
        keep(it.position.x == -42.8979m)
        keep(it.position.y == 46.29m)
        keep(it.orientation.yaw == -2.68793rad)
    sedan_route_point_146: pose_3d with:
        keep(it.position.x == -43.5369m)
        keep(it.position.y == 45.9967m)
        keep(it.orientation.yaw == -2.73472rad)
    sedan_route_point_147: pose_3d with:
        keep(it.position.x == -44.189m)
        keep(it.position.y == 45.7336m)
        keep(it.orientation.yaw == -2.78151rad)
    sedan_route_point_148: pose_3d with:
        keep(it.position.x == -44.8526m)
        keep(it.position.y == 45.5013m)
        keep(it.orientation.yaw == -2.8283rad)
    sedan_route_point_149: pose_3d with:
        keep(it.position.x == -45.5264m)
        keep(it.position.y == 45.3003m)
        keep(it.orientation.yaw == -2.87509rad)
    sedan_route_point_150: pose_3d with:
        keep(it.position.x == -46.2089m)
        keep(it.position.y == 45.1311m)
        keep(it.orientation.yaw == -2.92188rad)
    sedan_route_point_151: pose_3d with:
        keep(it.position.x == -46.8985m)
        keep(it.position.y == 44.9939m)
        keep(it.orientation.yaw == -2.96867rad)
    sedan_route_point_152: pose_3d with:
        keep(it.position.x == -47.5938m)
        keep(it.position.y == 44.8892m)
        keep(it.orientation.yaw == -3.01546rad)
    sedan_route_point_153: pose_3d with:
        keep(it.position.x == -48.2933m)
        keep(it.position.y == 44.8171m)
        keep(it.orientation.yaw == -3.06224rad)
    sedan_route_point_154: pose_3d with:
        keep(it.position.x == -48.9953m)
        keep(it.position.y == 44.7777m)
        keep(it.orientation.yaw == -3.10903rad)
    sedan_route_point_155: pose_3d with:
        keep(it.position.x == -49.6984m)
        keep(it.position.y == 44.7713m)
        keep(it.orientation.yaw == 3.12736rad)
    sedan_route_point_156: pose_3d with:
        keep(it.position.x == -50.4011m)
        keep(it.position.y == 44.7977m)
        keep(it.orientation.yaw == 3.08057rad)
    sedan_route_point_157: pose_3d with:
        keep(it.position.x == -51.1017m)
        keep(it.position.y == 44.857m)
        keep(it.orientation.yaw == 3.03378rad)
    sedan_route_point_158: pose_3d with:
        keep(it.position.x == -51.7988m)
        keep(it.position.y == 44.949m)
        keep(it.orientation.yaw == 2.98699rad)
    sedan_route_point_159: pose_3d with:
        keep(it.position.x == -52.4908m)
        keep(it.position.y == 45.0735m)
        keep(it.orientation.yaw == 2.9402rad)
    sedan_route_point_160: pose_3d with:
        keep(it.position.x == -53.1763m)
        keep(it.position.y == 45.2302m)
        keep(it.orientation.yaw == 2.89342rad)
    sedan_route_point_161: pose_3d with:
        keep(it.position.x == -53.8536m)
        keep(it.position.y == 45.4189m)
        keep(it.orientation.yaw == 2.84663rad)
    sedan_route_point_162: pose_3d with:
        keep(it.position.x == -54.5215m)
        keep(it.position.y == 45.6389m)
        keep(it.orientation.yaw == 2.79984rad)
    sedan_route_point_163: pose_3d with:
        keep(it.position.x == -55.1782m)
        keep(it.position.y == 45.89m)
        keep(it.orientation.yaw == 2.75305rad)
    sedan_route_point_164: pose_3d with:
        keep(it.position.x == -55.8226m)
        keep(it.position.y == 46.1716m)
        keep(it.orientation.yaw == 2.70626rad)
    sedan_route_point_165: pose_3d with:
        keep(it.position.x == -56.453m)
        keep(it.position.y == 46.4829m)
        keep(it.orientation.yaw == 2.65947rad)
    sedan_route_point_166: pose_3d with:
        keep(it.position.x == -57.0682m)
        keep(it.position.y == 46.8234m)
        keep(it.orientation.yaw == 2.61268rad)
    sedan_route_point_167: pose_3d with:
        keep(it.position.x == -57.6668m)
        keep(it.position.y == 47.1923m)
        keep(it.orientation.yaw == 2.56589rad)
    sedan_route_point_168: pose_3d with:
        keep(it.position.x == -58.2475m)
        keep(it.position.y == 47.5888m)
        keep(it.orientation.yaw == 2.5191rad)
    sedan_route_point_169: pose_3d with:
        keep(it.position.x == -58.809m)
        keep(it.position.y == 48.012m)
        keep(it.orientation.yaw == 2.47231rad)
    sedan_route_point_170: pose_3d with:
        keep(it.position.x == -59.3501m)
        keep(it.position.y == 48.4611m)
        keep(it.orientation.yaw == 2.42552rad)
    sedan_route_point_171: pose_3d with:
        keep(it.position.x == -59.8696m)
        keep(it.position.y == 48.9349m)
        keep(it.orientation.yaw == 2.37873rad)
    sedan_route_point_172: pose_3d with:
        keep(it.position.x == -60.3663m)
        keep(it.position.y == 49.4325m)
        keep(it.orientation.yaw == 2.33194rad)
    sedan_route_point_173: pose_3d with:
        keep(it.position.x == -60.8393m)
        keep(it.position.y == 49.9528m)
        keep(it.orientation.yaw == 2.28515rad)
    sedan_route_point_174: pose_3d with:
        keep(it.position.x == -61.2874m)
        keep(it.position.y == 50.4947m)
        keep(it.orientation.yaw == 2.23837rad)
    sedan_route_point_175: pose_3d with:
        keep(it.position.x == -61.7096m)
        keep(it.position.y == 51.0569m)
        keep(it.orientation.yaw == 2.19158rad)
    sedan_route_point_176: pose_3d with:
        keep(it.position.x == -62.1051m)
        keep(it.position.y == 51.6383m)
        keep(it.orientation.yaw == 2.15267rad)
    sedan_route_point_177: pose_3d with:
        keep(it.position.x == -62.3546m)
        keep(it.position.y == 52.0372m)
        keep(it.orientation.yaw == 2.11482rad)
    sedan_route_point_178: pose_3d with:
        keep(it.position.x == -62.5939m)
        keep(it.position.y == 52.4463m)
        keep(it.orientation.yaw == 2.08656rad)
    sedan_route_point_179: pose_3d with:
        keep(it.position.x == -62.8236m)
        keep(it.position.y == 52.8646m)
        keep(it.orientation.yaw == 2.06025rad)
    sedan_route_point_180: pose_3d with:
        keep(it.position.x == -63.0443m)
        keep(it.position.y == 53.2918m)
        keep(it.orientation.yaw == 2.03588rad)
    sedan_route_point_181: pose_3d with:
        keep(it.position.x == -63.2564m)
        keep(it.position.y == 53.727m)
        keep(it.orientation.yaw == 2.01347rad)
    sedan_route_point_182: pose_3d with:
        keep(it.position.x == -63.4605m)
        keep(it.position.y == 54.1697m)
        keep(it.orientation.yaw == 1.98385rad)
    sedan_route_point_183: pose_3d with:
        keep(it.position.x == -63.8475m)
        keep(it.position.y == 55.0759m)
        keep(it.orientation.yaw == 1.95879rad)
    sedan_route_point_184: pose_3d with:
        keep(it.position.x == -64.2111m)
        keep(it.position.y == 56.0063m)
        keep(it.orientation.yaw == 1.93153rad)
    sedan_route_point_185: pose_3d with:
        keep(it.position.x == -64.5575m)
        keep(it.position.y == 56.9579m)
        keep(it.orientation.yaw == 1.91207rad)
    sedan_route_point_186: pose_3d with:
        keep(it.position.x == -64.8935m)
        keep(it.position.y == 57.9276m)
        keep(it.orientation.yaw == 1.90041rad)
    sedan_route_point_187: pose_3d with:
        keep(it.position.x == -65.2263m)
        keep(it.position.y == 58.9129m)
        keep(it.orientation.yaw == 1.89691rad)
    sedan_route_point_188: pose_3d with:
        keep(it.position.x == -66.1739m)
        keep(it.position.y == 61.7136m)
        keep(it.orientation.yaw == 1.89928rad)
    sedan_route_point_189: pose_3d with:
        keep(it.position.x == -66.6594m)
        keep(it.position.y == 63.1175m)
        keep(it.orientation.yaw == 1.90778rad)
    sedan_route_point_190: pose_3d with:
        keep(it.position.x == -67.1577m)
        keep(it.position.y == 64.5218m)
        keep(it.orientation.yaw == 1.91717rad)
    sedan_route_point_191: pose_3d with:
        keep(it.position.x == -67.6727m)
        keep(it.position.y == 65.925m)
        keep(it.orientation.yaw == 1.92925rad)
    sedan_route_point_192: pose_3d with:
        keep(it.position.x == -68.2081m)
        keep(it.position.y == 67.3256m)
        keep(it.orientation.yaw == 1.94401rad)
    sedan_route_point_193: pose_3d with:
        keep(it.position.x == -68.7678m)
        keep(it.position.y == 68.7218m)
        keep(it.orientation.yaw == 1.96145rad)
    sedan_route_point_194: pose_3d with:
        keep(it.position.x == -69.3554m)
        keep(it.position.y == 70.1115m)
        keep(it.orientation.yaw == 1.97869rad)
    sedan_route_point_195: pose_3d with:
        keep(it.position.x == -69.8159m)
        keep(it.position.y == 71.1472m)
        keep(it.orientation.yaw == 1.99721rad)
    sedan_route_point_196: pose_3d with:
        keep(it.position.x == -70.2929m)
        keep(it.position.y == 72.1753m)
        keep(it.orientation.yaw == 2.01331rad)
    sedan_route_point_197: pose_3d with:
        keep(it.position.x == -70.7865m)
        keep(it.position.y == 73.1956m)
        keep(it.orientation.yaw == 2.02941rad)
    sedan_route_point_198: pose_3d with:
        keep(it.position.x == -71.2964m)
        keep(it.position.y == 74.2078m)
        keep(it.orientation.yaw == 2.04551rad)
    sedan_route_point_199: pose_3d with:
        keep(it.position.x == -71.8226m)
        keep(it.position.y == 75.2117m)
        keep(it.orientation.yaw == 2.06161rad)
    sedan_route_point_200: pose_3d with:
        keep(it.position.x == -72.3649m)
        keep(it.position.y == 76.207m)
        keep(it.orientation.yaw == 2.07771rad)
    sedan_route_point_201: pose_3d with:
        keep(it.position.x == -72.9231m)
        keep(it.position.y == 77.1934m)
        keep(it.orientation.yaw == 2.09381rad)
    sedan_route_point_202: pose_3d with:
        keep(it.position.x == -73.4971m)
        keep(it.position.y == 78.1707m)
        keep(it.orientation.yaw == 2.10991rad)
    sedan_route_point_203: pose_3d with:
        keep(it.position.x == -74.0867m)
        keep(it.position.y == 79.1386m)
        keep(it.orientation.yaw == 2.12601rad)
    sedan_route_point_204: pose_3d with:
        keep(it.position.x == -74.6919m)
        keep(it.position.y == 80.0969m)
        keep(it.orientation.yaw == 2.14211rad)
    sedan_route_point_205: pose_3d with:
        keep(it.position.x == -75.3124m)
        keep(it.position.y == 81.0454m)
        keep(it.orientation.yaw == 2.15821rad)
    sedan_route_point_206: pose_3d with:
        keep(it.position.x == -75.9482m)
        keep(it.position.y == 81.9837m)
        keep(it.orientation.yaw == 2.17431rad)
    sedan_route_point_207: pose_3d with:
        keep(it.position.x == -76.5989m)
        keep(it.position.y == 82.9117m)
        keep(it.orientation.yaw == 2.19041rad)
    sedan_route_point_208: pose_3d with:
        keep(it.position.x == -77.2645m)
        keep(it.position.y == 83.8291m)
        keep(it.orientation.yaw == 2.20126rad)
    sedan_route_point_209: pose_3d with:
        keep(it.position.x == -77.4998m)
        keep(it.position.y == 84.1461m)
        keep(it.orientation.yaw == 2.21456rad)
    sedan_route_point_210: pose_3d with:
        keep(it.position.x == -77.9448m)
        keep(it.position.y == 84.7356m)
        keep(it.orientation.yaw == 2.22541rad)
    sedan_route_point_211: pose_3d with:
        keep(it.position.x == -78.6396m)
        keep(it.position.y == 85.6311m)
        keep(it.orientation.yaw == 2.2387rad)
    sedan_route_point_212: pose_3d with:
        keep(it.position.x == -79.3487m)
        keep(it.position.y == 86.5153m)
        keep(it.orientation.yaw == 2.2548rad)
    sedan_route_point_213: pose_3d with:
        keep(it.position.x == -80.0719m)
        keep(it.position.y == 87.3879m)
        keep(it.orientation.yaw == 2.2709rad)
    sedan_route_point_214: pose_3d with:
        keep(it.position.x == -80.8091m)
        keep(it.position.y == 88.2488m)
        keep(it.orientation.yaw == 2.287rad)
    sedan_route_point_215: pose_3d with:
        keep(it.position.x == -81.5601m)
        keep(it.position.y == 89.0977m)
        keep(it.orientation.yaw == 2.3031rad)
    sedan_route_point_216: pose_3d with:
        keep(it.position.x == -82.3246m)
        keep(it.position.y == 89.9345m)
        keep(it.orientation.yaw == 2.3192rad)
    sedan_route_point_217: pose_3d with:
        keep(it.position.x == -83.1026m)
        keep(it.position.y == 90.7588m)
        keep(it.orientation.yaw == 2.3353rad)
    sedan_route_point_218: pose_3d with:
        keep(it.position.x == -83.8936m)
        keep(it.position.y == 91.5704m)
        keep(it.orientation.yaw == 2.3514rad)
    sedan_route_point_219: pose_3d with:
        keep(it.position.x == -84.6977m)
        keep(it.position.y == 92.3692m)
        keep(it.orientation.yaw == 2.3675rad)
    sedan_route_point_220: pose_3d with:
        keep(it.position.x == -85.5145m)
        keep(it.position.y == 93.155m)
        keep(it.orientation.yaw == 2.3836rad)
    sedan_route_point_221: pose_3d with:
        keep(it.position.x == -86.3438m)
        keep(it.position.y == 93.9275m)
        keep(it.orientation.yaw == 2.3997rad)
    sedan_route_point_222: pose_3d with:
        keep(it.position.x == -87.1855m)
        keep(it.position.y == 94.6866m)
        keep(it.orientation.yaw == 2.4158rad)
    sedan_route_point_223: pose_3d with:
        keep(it.position.x == -88.0393m)
        keep(it.position.y == 95.432m)
        keep(it.orientation.yaw == 2.4319rad)
    sedan_route_point_224: pose_3d with:
        keep(it.position.x == -88.905m)
        keep(it.position.y == 96.1636m)
        keep(it.orientation.yaw == 2.448rad)
    sedan_route_point_225: pose_3d with:
        keep(it.position.x == -89.7823m)
        keep(it.position.y == 96.8812m)
        keep(it.orientation.yaw == 2.4641rad)
    sedan_route_point_226: pose_3d with:
        keep(it.position.x == -90.6711m)
        keep(it.position.y == 97.5845m)
        keep(it.orientation.yaw == 2.4802rad)
    sedan_route_point_227: pose_3d with:
        keep(it.position.x == -91.5711m)
        keep(it.position.y == 98.2734m)
        keep(it.orientation.yaw == 2.49872rad)
    sedan_route_point_228: pose_3d with:
        keep(it.position.x == -92.7858m)
        keep(it.position.y == 99.1685m)
        keep(it.orientation.yaw == 2.51596rad)
    sedan_route_point_229: pose_3d with:
        keep(it.position.x == -94.0133m)
        keep(it.position.y == 100.038m)
        keep(it.orientation.yaw == 2.5334rad)
    sedan_route_point_230: pose_3d with:
        keep(it.position.x == -95.2508m)
        keep(it.position.y == 100.885m)
        keep(it.orientation.yaw == 2.54816rad)
    sedan_route_point_231: pose_3d with:
        keep(it.position.x == -96.4956m)
        keep(it.position.y == 101.712m)
        keep(it.orientation.yaw == 2.56024rad)
    sedan_route_point_232: pose_3d with:
        keep(it.position.x == -97.7452m)
        keep(it.position.y == 102.524m)
        keep(it.orientation.yaw == 2.56963rad)
    sedan_route_point_233: pose_3d with:
        keep(it.position.x == -98.9975m)
        keep(it.position.y == 103.323m)
        keep(it.orientation.yaw == 2.57813rad)
    sedan_route_point_234: pose_3d with:
        keep(it.position.x == -101.501m)
        keep(it.position.y == 104.896m)
        keep(it.orientation.yaw == 2.58133rad)
    sedan_route_point_235: pose_3d with:
        keep(it.position.x == -104.357m)
        keep(it.position.y == 106.685m)
        keep(it.orientation.yaw == 2.58216rad)
    sedan_route_point_236: pose_3d with:
        keep(it.position.x == -104.669m)
        keep(it.position.y == 106.88m)
        keep(it.orientation.yaw == 2.569rad)
    sedan_route_point_237: pose_3d with:
        keep(it.position.x == -104.97m)
        keep(it.position.y == 107.08m)
        keep(it.orientation.yaw == 2.52901rad)
    sedan_route_point_238: pose_3d with:
        keep(it.position.x == -105.26m)
        keep(it.position.y == 107.296m)
        keep(it.orientation.yaw == 2.47586rad)
    sedan_route_point_239: pose_3d with:
        keep(it.position.x == -105.538m)
        keep(it.position.y == 107.526m)
        keep(it.orientation.yaw == 2.4227rad)
    sedan_route_point_240: pose_3d with:
        keep(it.position.x == -105.804m)
        keep(it.position.y == 107.771m)
        keep(it.orientation.yaw == 2.3674rad)
    sedan_route_point_241: pose_3d with:
        keep(it.position.x == -106.076m)
        keep(it.position.y == 108.052m)
        keep(it.orientation.yaw == 2.3121rad)
    sedan_route_point_242: pose_3d with:
        keep(it.position.x == -106.331m)
        keep(it.position.y == 108.347m)
        keep(it.orientation.yaw == 2.25466rad)
    sedan_route_point_243: pose_3d with:
        keep(it.position.x == -106.569m)
        keep(it.position.y == 108.657m)
        keep(it.orientation.yaw == 2.19721rad)
    sedan_route_point_244: pose_3d with:
        keep(it.position.x == -106.789m)
        keep(it.position.y == 108.98m)
        keep(it.orientation.yaw == 2.13976rad)
    sedan_route_point_245: pose_3d with:
        keep(it.position.x == -106.989m)
        keep(it.position.y == 109.315m)
        keep(it.orientation.yaw == 2.08232rad)
    sedan_route_point_246: pose_3d with:
        keep(it.position.x == -107.171m)
        keep(it.position.y == 109.66m)
        keep(it.orientation.yaw == 2.02487rad)
    sedan_route_point_247: pose_3d with:
        keep(it.position.x == -107.332m)
        keep(it.position.y == 110.016m)
        keep(it.orientation.yaw == 1.96743rad)
    sedan_route_point_248: pose_3d with:
        keep(it.position.x == -107.472m)
        keep(it.position.y == 110.381m)
        keep(it.orientation.yaw == 1.93228rad)
    sedan_route_point_249: pose_3d with:
        keep(it.position.x == -107.501m)
        keep(it.position.y == 110.463m)
        keep(it.orientation.yaw == 1.87262rad)
    sedan_route_point_250: pose_3d with:
        keep(it.position.x == -107.623m)
        keep(it.position.y == 110.866m)
        keep(it.orientation.yaw == 1.83526rad)
    sedan_route_point_251: pose_3d with:
        keep(it.position.x == -107.721m)
        keep(it.position.y == 111.275m)
        keep(it.orientation.yaw == 1.7734rad)
    sedan_route_point_252: pose_3d with:
        keep(it.position.x == -107.793m)
        keep(it.position.y == 111.689m)
        keep(it.orientation.yaw == 1.71154rad)
    sedan_route_point_253: pose_3d with:
        keep(it.position.x == -107.839m)
        keep(it.position.y == 112.107m)
        keep(it.orientation.yaw == 1.64967rad)
    sedan_route_point_254: pose_3d with:
        keep(it.position.x == -107.859m)
        keep(it.position.y == 112.527m)
        keep(it.orientation.yaw == 1.58781rad)
    sedan_route_point_255: pose_3d with:
        keep(it.position.x == -107.853m)
        keep(it.position.y == 112.948m)
        keep(it.orientation.yaw == 1.52595rad)
    sedan_route_point_256: pose_3d with:
        keep(it.position.x == -107.821m)
        keep(it.position.y == 113.367m)
        keep(it.orientation.yaw == 1.46408rad)
    sedan_route_point_257: pose_3d with:
        keep(it.position.x == -107.763m)
        keep(it.position.y == 113.784m)
        keep(it.orientation.yaw == 1.4094rad)
    sedan_route_point_258: pose_3d with:
        keep(it.position.x == -107.702m)
        keep(it.position.y == 114.101m)
        keep(it.orientation.yaw == 1.35471rad)
    sedan_route_point_259: pose_3d with:
        keep(it.position.x == -107.625m)
        keep(it.position.y == 114.415m)
        keep(it.orientation.yaw == 1.3072rad)
    sedan_route_point_260: pose_3d with:
        keep(it.position.x == -107.533m)
        keep(it.position.y == 114.724m)
        keep(it.orientation.yaw == 1.25968rad)
    sedan_route_point_261: pose_3d with:
        keep(it.position.x == -107.427m)
        keep(it.position.y == 115.03m)
        keep(it.orientation.yaw == 1.22327rad)
    sedan_route_point_262: pose_3d with:
        keep(it.position.x == -107.298m)
        keep(it.position.y == 115.374m)
        keep(it.orientation.yaw == 1.22001rad)
    sedan_route_point_263: pose_3d with:
        keep(it.position.x == -107.038m)
        keep(it.position.y == 116.094m)
        keep(it.orientation.yaw == 1.23533rad)
    sedan_route_point_264: pose_3d with:
        keep(it.position.x == -106.795m)
        keep(it.position.y == 116.816m)
        keep(it.orientation.yaw == 1.25848rad)
    sedan_route_point_265: pose_3d with:
        keep(it.position.x == -106.569m)
        keep(it.position.y == 117.544m)
        keep(it.orientation.yaw == 1.28158rad)
    sedan_route_point_266: pose_3d with:
        keep(it.position.x == -106.361m)
        keep(it.position.y == 118.276m)
        keep(it.orientation.yaw == 1.3095rad)
    sedan_route_point_267: pose_3d with:
        keep(it.position.x == -106.094m)
        keep(it.position.y == 119.323m)
        keep(it.orientation.yaw == 1.33742rad)
    sedan_route_point_268: pose_3d with:
        keep(it.position.x == -105.861m)
        keep(it.position.y == 120.377m)
        keep(it.orientation.yaw == 1.37016rad)
    sedan_route_point_269: pose_3d with:
        keep(it.position.x == -105.663m)
        keep(it.position.y == 121.439m)
        keep(it.orientation.yaw == 1.40291rad)
    sedan_route_point_270: pose_3d with:
        keep(it.position.x == -105.5m)
        keep(it.position.y == 122.506m)
        keep(it.orientation.yaw == 1.43565rad)
    sedan_route_point_271: pose_3d with:
        keep(it.position.x == -105.372m)
        keep(it.position.y == 123.579m)
        keep(it.orientation.yaw == 1.4684rad)
    sedan_route_point_272: pose_3d with:
        keep(it.position.x == -105.28m)
        keep(it.position.y == 124.655m)
        keep(it.orientation.yaw == 1.50114rad)
    sedan_route_point_273: pose_3d with:
        keep(it.position.x == -105.222m)
        keep(it.position.y == 125.733m)
        keep(it.orientation.yaw == 1.53389rad)
    sedan_route_point_274: pose_3d with:
        keep(it.position.x == -105.2m)
        keep(it.position.y == 126.813m)
        keep(it.orientation.yaw == 1.56662rad)
    sedan_route_point_275: pose_3d with:
        keep(it.position.x == -105.213m)
        keep(it.position.y == 127.892m)
        keep(it.orientation.yaw == 1.59935rad)
    sedan_route_point_276: pose_3d with:
        keep(it.position.x == -105.262m)
        keep(it.position.y == 128.97m)
        keep(it.orientation.yaw == 1.63208rad)
    sedan_route_point_277: pose_3d with:
        keep(it.position.x == -105.345m)
        keep(it.position.y == 130.046m)
        keep(it.orientation.yaw == 1.6648rad)
    sedan_route_point_278: pose_3d with:
        keep(it.position.x == -105.464m)
        keep(it.position.y == 131.118m)
        keep(it.orientation.yaw == 1.69752rad)
    sedan_route_point_279: pose_3d with:
        keep(it.position.x == -105.618m)
        keep(it.position.y == 132.186m)
        keep(it.orientation.yaw == 1.73024rad)
    sedan_route_point_280: pose_3d with:
        keep(it.position.x == -105.807m)
        keep(it.position.y == 133.249m)
        keep(it.orientation.yaw == 1.76296rad)
    sedan_route_point_281: pose_3d with:
        keep(it.position.x == -106.03m)
        keep(it.position.y == 134.304m)
        keep(it.orientation.yaw == 1.79568rad)
    sedan_route_point_282: pose_3d with:
        keep(it.position.x == -106.288m)
        keep(it.position.y == 135.352m)
        keep(it.orientation.yaw == 1.82667rad)
    sedan_route_point_283: pose_3d with:
        keep(it.position.x == -106.547m)
        keep(it.position.y == 136.282m)
        keep(it.orientation.yaw == 1.85767rad)
    sedan_route_point_284: pose_3d with:
        keep(it.position.x == -106.834m)
        keep(it.position.y == 137.203m)
        keep(it.orientation.yaw == 1.88693rad)
    sedan_route_point_285: pose_3d with:
        keep(it.position.x == -107.148m)
        keep(it.position.y == 138.116m)
        keep(it.orientation.yaw == 1.9162rad)
    sedan_route_point_286: pose_3d with:
        keep(it.position.x == -107.488m)
        keep(it.position.y == 139.02m)
        keep(it.orientation.yaw == 1.94546rad)
    sedan_route_point_287: pose_3d with:
        keep(it.position.x == -107.854m)
        keep(it.position.y == 139.912m)
        keep(it.orientation.yaw == 1.97473rad)
    sedan_route_point_288: pose_3d with:
        keep(it.position.x == -108.246m)
        keep(it.position.y == 140.794m)
        keep(it.orientation.yaw == 2.00399rad)
    sedan_route_point_289: pose_3d with:
        keep(it.position.x == -108.664m)
        keep(it.position.y == 141.664m)
        keep(it.orientation.yaw == 2.03326rad)
    sedan_route_point_290: pose_3d with:
        keep(it.position.x == -109.107m)
        keep(it.position.y == 142.522m)
        keep(it.orientation.yaw == 2.0638rad)
    sedan_route_point_291: pose_3d with:
        keep(it.position.x == -109.617m)
        keep(it.position.y == 143.438m)
        keep(it.orientation.yaw == 2.09428rad)
    sedan_route_point_292: pose_3d with:
        keep(it.position.x == -110.154m)
        keep(it.position.y == 144.335m)
        keep(it.orientation.yaw == 2.1243rad)
    sedan_route_point_293: pose_3d with:
        keep(it.position.x == -110.656m)
        keep(it.position.y == 145.12m)
        keep(it.orientation.yaw == 2.15427rad)
    sedan_route_point_294: pose_3d with:
        keep(it.position.x == -111.18m)
        keep(it.position.y == 145.89m)
        keep(it.orientation.yaw == 2.18251rad)
    sedan_route_point_295: pose_3d with:
        keep(it.position.x == -111.726m)
        keep(it.position.y == 146.644m)
        keep(it.orientation.yaw == 2.21074rad)
    sedan_route_point_296: pose_3d with:
        keep(it.position.x == -112.292m)
        keep(it.position.y == 147.383m)
        keep(it.orientation.yaw == 2.23898rad)
    sedan_route_point_297: pose_3d with:
        keep(it.position.x == -112.879m)
        keep(it.position.y == 148.106m)
        keep(it.orientation.yaw == 2.26722rad)
    sedan_route_point_298: pose_3d with:
        keep(it.position.x == -113.487m)
        keep(it.position.y == 148.812m)
        keep(it.orientation.yaw == 2.29546rad)
    sedan_route_point_299: pose_3d with:
        keep(it.position.x == -114.114m)
        keep(it.position.y == 149.501m)
        keep(it.orientation.yaw == 2.3237rad)
    sedan_route_point_300: pose_3d with:
        keep(it.position.x == -114.76m)
        keep(it.position.y == 150.171m)
        keep(it.orientation.yaw == 2.3496rad)
    sedan_route_point_301: pose_3d with:
        keep(it.position.x == -115.314m)
        keep(it.position.y == 150.716m)
        keep(it.orientation.yaw == 2.37545rad)
    sedan_route_point_302: pose_3d with:
        keep(it.position.x == -115.878m)
        keep(it.position.y == 151.247m)
        keep(it.orientation.yaw == 2.39897rad)
    sedan_route_point_303: pose_3d with:
        keep(it.position.x == -116.454m)
        keep(it.position.y == 151.763m)
        keep(it.orientation.yaw == 2.42244rad)
    sedan_route_point_304: pose_3d with:
        keep(it.position.x == -117.043m)
        keep(it.position.y == 152.266m)
        keep(it.orientation.yaw == 2.45114rad)
    sedan_route_point_305: pose_3d with:
        keep(it.position.x == -117.914m)
        keep(it.position.y == 152.969m)
        keep(it.orientation.yaw == 2.47984rad)
    sedan_route_point_306: pose_3d with:
        keep(it.position.x == -118.808m)
        keep(it.position.y == 153.641m)
        keep(it.orientation.yaw == 2.50812rad)
    sedan_route_point_307: pose_3d with:
        keep(it.position.x == -119.417m)
        keep(it.position.y == 154.073m)
        keep(it.orientation.yaw == 2.53639rad)
    sedan_route_point_308: pose_3d with:
        keep(it.position.x == -120.035m)
        keep(it.position.y == 154.49m)
        keep(it.orientation.yaw == 2.55901rad)
    sedan_route_point_309: pose_3d with:
        keep(it.position.x == -120.662m)
        keep(it.position.y == 154.893m)
        keep(it.orientation.yaw == 2.58161rad)
    sedan_route_point_310: pose_3d with:
        keep(it.position.x == -121.298m)
        keep(it.position.y == 155.282m)
        keep(it.orientation.yaw == 2.60422rad)
    sedan_route_point_311: pose_3d with:
        keep(it.position.x == -121.943m)
        keep(it.position.y == 155.656m)
        keep(it.orientation.yaw == 2.62682rad)
    sedan_route_point_312: pose_3d with:
        keep(it.position.x == -122.596m)
        keep(it.position.y == 156.016m)
        keep(it.orientation.yaw == 2.64942rad)
    sedan_route_point_313: pose_3d with:
        keep(it.position.x == -123.257m)
        keep(it.position.y == 156.361m)
        keep(it.orientation.yaw == 2.67203rad)
    sedan_route_point_314: pose_3d with:
        keep(it.position.x == -123.925m)
        keep(it.position.y == 156.69m)
        keep(it.orientation.yaw == 2.69463rad)
    sedan_route_point_315: pose_3d with:
        keep(it.position.x == -124.601m)
        keep(it.position.y == 157.005m)
        keep(it.orientation.yaw == 2.71724rad)
    sedan_route_point_316: pose_3d with:
        keep(it.position.x == -125.284m)
        keep(it.position.y == 157.304m)
        keep(it.orientation.yaw == 2.73984rad)
    sedan_route_point_317: pose_3d with:
        keep(it.position.x == -125.973m)
        keep(it.position.y == 157.588m)
        keep(it.orientation.yaw == 2.76245rad)
    sedan_route_point_318: pose_3d with:
        keep(it.position.x == -126.669m)
        keep(it.position.y == 157.856m)
        keep(it.orientation.yaw == 2.78505rad)
    sedan_route_point_319: pose_3d with:
        keep(it.position.x == -127.37m)
        keep(it.position.y == 158.108m)
        keep(it.orientation.yaw == 2.80766rad)
    sedan_route_point_320: pose_3d with:
        keep(it.position.x == -128.077m)
        keep(it.position.y == 158.345m)
        keep(it.orientation.yaw == 2.83026rad)
    sedan_route_point_321: pose_3d with:
        keep(it.position.x == -128.789m)
        keep(it.position.y == 158.565m)
        keep(it.orientation.yaw == 2.85286rad)
    sedan_route_point_322: pose_3d with:
        keep(it.position.x == -129.506m)
        keep(it.position.y == 158.769m)
        keep(it.orientation.yaw == 2.88054rad)
    sedan_route_point_323: pose_3d with:
        keep(it.position.x == -130.553m)
        keep(it.position.y == 159.036m)
        keep(it.orientation.yaw == 2.90821rad)
    sedan_route_point_324: pose_3d with:
        keep(it.position.x == -131.607m)
        keep(it.position.y == 159.269m)
        keep(it.orientation.yaw == 2.94096rad)
    sedan_route_point_325: pose_3d with:
        keep(it.position.x == -132.669m)
        keep(it.position.y == 159.467m)
        keep(it.orientation.yaw == 2.9737rad)
    sedan_route_point_326: pose_3d with:
        keep(it.position.x == -133.736m)
        keep(it.position.y == 159.63m)
        keep(it.orientation.yaw == 3.00645rad)
    sedan_route_point_327: pose_3d with:
        keep(it.position.x == -134.809m)
        keep(it.position.y == 159.758m)
        keep(it.orientation.yaw == 3.03919rad)
    sedan_route_point_328: pose_3d with:
        keep(it.position.x == -135.885m)
        keep(it.position.y == 159.85m)
        keep(it.orientation.yaw == 3.07194rad)
    sedan_route_point_329: pose_3d with:
        keep(it.position.x == -136.963m)
        keep(it.position.y == 159.908m)
        keep(it.orientation.yaw == 3.10468rad)
    sedan_route_point_330: pose_3d with:
        keep(it.position.x == -138.043m)
        keep(it.position.y == 159.93m)
        keep(it.orientation.yaw == 3.13742rad)
    sedan_route_point_331: pose_3d with:
        keep(it.position.x == -139.122m)
        keep(it.position.y == 159.917m)
        keep(it.orientation.yaw == -3.11303rad)
    sedan_route_point_332: pose_3d with:
        keep(it.position.x == -140.2m)
        keep(it.position.y == 159.868m)
        keep(it.orientation.yaw == -3.08031rad)
    sedan_route_point_333: pose_3d with:
        keep(it.position.x == -141.276m)
        keep(it.position.y == 159.785m)
        keep(it.orientation.yaw == -3.04759rad)
    sedan_route_point_334: pose_3d with:
        keep(it.position.x == -142.348m)
        keep(it.position.y == 159.666m)
        keep(it.orientation.yaw == -3.01487rad)
    sedan_route_point_335: pose_3d with:
        keep(it.position.x == -143.416m)
        keep(it.position.y == 159.512m)
        keep(it.orientation.yaw == -2.98215rad)
    sedan_route_point_336: pose_3d with:
        keep(it.position.x == -144.479m)
        keep(it.position.y == 159.323m)
        keep(it.orientation.yaw == -2.94943rad)
    sedan_route_point_337: pose_3d with:
        keep(it.position.x == -145.534m)
        keep(it.position.y == 159.1m)
        keep(it.orientation.yaw == -2.91671rad)
    sedan_route_point_338: pose_3d with:
        keep(it.position.x == -146.582m)
        keep(it.position.y == 158.842m)
        keep(it.orientation.yaw == -2.88872rad)
    sedan_route_point_339: pose_3d with:
        keep(it.position.x == -147.322m)
        keep(it.position.y == 158.638m)
        keep(it.orientation.yaw == -2.86073rad)
    sedan_route_point_340: pose_3d with:
        keep(it.position.x == -148.056m)
        keep(it.position.y == 158.417m)
        keep(it.orientation.yaw == -2.83747rad)
    sedan_route_point_341: pose_3d with:
        keep(it.position.x == -148.785m)
        keep(it.position.y == 158.179m)
        keep(it.orientation.yaw == -2.81421rad)
    sedan_route_point_342: pose_3d with:
        keep(it.position.x == -149.509m)
        keep(it.position.y == 157.923m)
        keep(it.orientation.yaw == -2.79095rad)
    sedan_route_point_343: pose_3d with:
        keep(it.position.x == -150.226m)
        keep(it.position.y == 157.652m)
        keep(it.orientation.yaw == -2.76769rad)
    sedan_route_point_344: pose_3d with:
        keep(it.position.x == -150.937m)
        keep(it.position.y == 157.363m)
        keep(it.orientation.yaw == -2.74443rad)
    sedan_route_point_345: pose_3d with:
        keep(it.position.x == -151.641m)
        keep(it.position.y == 157.058m)
        keep(it.orientation.yaw == -2.72117rad)
    sedan_route_point_346: pose_3d with:
        keep(it.position.x == -152.337m)
        keep(it.position.y == 156.737m)
        keep(it.orientation.yaw == -2.69791rad)
    sedan_route_point_347: pose_3d with:
        keep(it.position.x == -153.026m)
        keep(it.position.y == 156.4m)
        keep(it.orientation.yaw == -2.67465rad)
    sedan_route_point_348: pose_3d with:
        keep(it.position.x == -153.707m)
        keep(it.position.y == 156.046m)
        keep(it.orientation.yaw == -2.65139rad)
    sedan_route_point_349: pose_3d with:
        keep(it.position.x == -154.38m)
        keep(it.position.y == 155.677m)
        keep(it.orientation.yaw == -2.62813rad)
    sedan_route_point_350: pose_3d with:
        keep(it.position.x == -155.043m)
        keep(it.position.y == 155.293m)
        keep(it.orientation.yaw == -2.60487rad)
    sedan_route_point_351: pose_3d with:
        keep(it.position.x == -155.698m)
        keep(it.position.y == 154.893m)
        keep(it.orientation.yaw == -2.58162rad)
    sedan_route_point_352: pose_3d with:
        keep(it.position.x == -156.343m)
        keep(it.position.y == 154.478m)
        keep(it.orientation.yaw == -2.55836rad)
    sedan_route_point_353: pose_3d with:
        keep(it.position.x == -156.978m)
        keep(it.position.y == 154.048m)
        keep(it.orientation.yaw == -2.5351rad)
    sedan_route_point_354: pose_3d with:
        keep(it.position.x == -157.604m)
        keep(it.position.y == 153.604m)
        keep(it.orientation.yaw == -2.51946rad)
    sedan_route_point_355: pose_3d with:
        keep(it.position.x == -157.929m)
        keep(it.position.y == 153.366m)
        keep(it.orientation.yaw == -2.52305rad)
    sedan_route_point_356: pose_3d with:
        keep(it.position.x == -158.204m)
        keep(it.position.y == 153.177m)
        keep(it.orientation.yaw == -2.56136rad)
    sedan_route_point_357: pose_3d with:
        keep(it.position.x == -158.487m)
        keep(it.position.y == 153.001m)
        keep(it.orientation.yaw == -2.61089rad)
    sedan_route_point_358: pose_3d with:
        keep(it.position.x == -158.779m)
        keep(it.position.y == 152.839m)
        keep(it.orientation.yaw == -2.66042rad)
    sedan_route_point_359: pose_3d with:
        keep(it.position.x == -159.078m)
        keep(it.position.y == 152.692m)
        keep(it.orientation.pitch == 0.00159843rad)
        keep(it.orientation.yaw == -2.71371rad)
    sedan_route_point_360: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.0011468m)
        keep(it.orientation.pitch == 0.00671628rad)
        keep(it.orientation.yaw == -2.73847rad)
    sedan_route_point_361: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00258014m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_362: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00351178m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_363: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00403149m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_364: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00430484m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_365: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00444489m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_366: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00451576m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_367: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.0045514m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_368: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00456928m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_369: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00457823m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_370: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00458271m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_371: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00458495m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_372: pose_3d with:
        keep(it.position.x == -159.431m)
        keep(it.position.y == 152.541m)
        keep(it.position.z == 0.00458607m)
        keep(it.orientation.pitch == 5.8329e-06rad)
        keep(it.orientation.yaw == -2.79552rad)
    sedan_route_point_373: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.00458719m)
        keep(it.orientation.pitch == 0.0067192rad)
        keep(it.orientation.yaw == -2.79552rad)
    sedan_route_point_374: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.00716733m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_375: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.00867225m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_376: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.00947867m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_377: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.00989541m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_378: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0101072m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_379: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0102139m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_380: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0102675m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_381: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0102943m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_382: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0103077m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_383: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0103145m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_384: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0103178m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_385: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0103195m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_386: pose_3d with:
        keep(it.position.x == -159.793m)
        keep(it.position.y == 152.411m)
        keep(it.position.z == 0.0103208m)
        keep(it.orientation.pitch == 4.37503e-06rad)
        keep(it.orientation.yaw == -2.85256rad)
    sedan_route_point_387: pose_3d with:
        keep(it.position.x == -160.161m)
        keep(it.position.y == 152.302m)
        keep(it.position.z == 0.0103212m)
        keep(it.orientation.pitch == 0.010453rad)
        keep(it.orientation.yaw == -2.88109rad)
    sedan_route_point_388: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0183488m)
        keep(it.orientation.pitch == 0.033583rad)
        keep(it.orientation.yaw == -2.90961rad)
    sedan_route_point_389: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0232225m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_390: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.025874m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_391: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0272538m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_392: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0279573m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_393: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0283125m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_394: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0284909m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_395: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0285804m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_396: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286251m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_397: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286475m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_398: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286587m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_399: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286643m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_400: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286671m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_401: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286685m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_402: pose_3d with:
        keep(it.position.x == -160.535m)
        keep(it.position.y == 152.213m)
        keep(it.position.z == 0.0286696m)
        keep(it.orientation.pitch == 3.64597e-06rad)
        keep(it.orientation.yaw == -2.96665rad)
    sedan_route_point_403: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0286699m)
        keep(it.orientation.pitch == 0.0156728rad)
        keep(it.orientation.yaw == -2.96665rad)
    sedan_route_point_404: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0346905m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_405: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0379152m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_406: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0395818m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_407: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0404287m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_408: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0408556m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_409: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0410699m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_410: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0411772m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_411: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0412309m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_412: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0412578m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_413: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0412713m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_414: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.041278m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_415: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0412813m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_416: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.041283m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_417: pose_3d with:
        keep(it.position.x == -160.913m)
        keep(it.position.y == 152.146m)
        keep(it.position.z == 0.0412843m)
        keep(it.orientation.pitch == 4.37517e-06rad)
        keep(it.orientation.yaw == -3.0237rad)
    sedan_route_point_418: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0412847m)
        keep(it.orientation.pitch == 0.0186581rad)
        keep(it.orientation.yaw == -3.0237rad)
    sedan_route_point_419: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.048452m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_420: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0522501m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_421: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0542034m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_422: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0551937m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_423: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0556922m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_424: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0559424m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_425: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0560676m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_426: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561303m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_427: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561617m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_428: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561774m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_429: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561852m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_430: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561892m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_431: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561911m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_432: pose_3d with:
        keep(it.position.x == -161.295m)
        keep(it.position.y == 152.101m)
        keep(it.position.z == 0.0561926m)
        keep(it.orientation.pitch == 5.10437e-06rad)
        keep(it.orientation.yaw == -3.08075rad)
    sedan_route_point_433: pose_3d with:
        keep(it.position.x == -161.678m)
        keep(it.position.y == 152.078m)
        keep(it.position.z == 0.0561931m)
        keep(it.orientation.pitch == 0.0223987rad)
        keep(it.orientation.yaw == -3.10927rad)
    sedan_route_point_434: pose_3d with:
        keep(it.position.x == -162.062m)
        keep(it.position.y == 152.076m)
        keep(it.position.z == 0.073395m)
        keep(it.orientation.pitch == 0.0451557rad)
        keep(it.orientation.yaw == 3.14087rad)
    sedan_route_point_435: pose_3d with:
        keep(it.position.x == -162.123m)
        keep(it.position.y == 152.078m)
        keep(it.position.z == 0.0762873m)
        keep(it.orientation.pitch == 0.0446124rad)
        keep(it.orientation.yaw == 3.08236rad)
    sedan_route_point_436: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.0941238m)
        keep(it.orientation.pitch == 0.0640482rad)
        keep(it.orientation.yaw == 3.07785rad)
    sedan_route_point_437: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.10215m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_438: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.10594m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_439: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.107779m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_440: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.108685m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_441: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109135m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_442: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109359m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_443: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.10947m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_444: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109526m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_445: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109554m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_446: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109568m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_447: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109575m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_448: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109579m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_449: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.10958m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_450: pose_3d with:
        keep(it.position.x == -162.526m)
        keep(it.position.y == 152.104m)
        keep(it.position.z == 0.109582m)
        keep(it.orientation.pitch == 4.31619e-06rad)
        keep(it.orientation.yaw == 3.01787rad)
    sedan_route_point_451: pose_3d with:
        keep(it.position.x == -162.927m)
        keep(it.position.y == 152.154m)
        keep(it.position.z == 0.109582m)
        keep(it.orientation.pitch == 0.0162038rad)
        keep(it.orientation.yaw == 2.98788rad)
    sedan_route_point_452: pose_3d with:
        keep(it.position.x == -163.324m)
        keep(it.position.y == 152.228m)
        keep(it.position.z == 0.122662m)
        keep(it.orientation.pitch == 0.0294605rad)
        keep(it.orientation.yaw == 2.92791rad)
    sedan_route_point_453: pose_3d with:
        keep(it.position.x == -163.716m)
        keep(it.position.y == 152.325m)
        keep(it.position.z == 0.133364m)
        keep(it.orientation.pitch == 0.0235684rad)
        keep(it.orientation.yaw == 2.86793rad)
    sedan_route_point_454: pose_3d with:
        keep(it.position.x == -164.101m)
        keep(it.position.y == 152.446m)
        keep(it.position.z == 0.141688m)
        keep(it.orientation.pitch == 0.0176763rad)
        keep(it.orientation.yaw == 2.80796rad)
    sedan_route_point_455: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.147633m)
        keep(it.orientation.pitch == 0.0198773rad)
        keep(it.orientation.yaw == 2.77797rad)
    sedan_route_point_456: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.149714m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_457: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.150531m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_458: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.150884m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_459: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151047m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_460: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151125m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_461: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151163m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_462: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151182m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_463: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151191m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_464: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151196m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_465: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151198m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_466: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.151199m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_467: pose_3d with:
        keep(it.position.x == -164.479m)
        keep(it.position.y == 152.589m)
        keep(it.position.z == 0.1512m)
        keep(it.orientation.pitch == 2.87813e-06rad)
        keep(it.orientation.yaw == 2.71799rad)
    sedan_route_point_468: pose_3d with:
        keep(it.position.x == -164.847m)
        keep(it.position.y == 152.755m)
        keep(it.position.z == 0.1512m)
        keep(it.orientation.pitch == 0.00147321rad)
        keep(it.orientation.yaw == 2.68801rad)
    sedan_route_point_469: pose_3d with:
        keep(it.position.x == -165.204m)
        keep(it.position.y == 152.943m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.pitch == 0.00166782rad)
        keep(it.orientation.yaw == 2.63504rad)
    sedan_route_point_470: pose_3d with:
        keep(it.position.x == -165.47m)
        keep(it.position.y == 153.101m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.58208rad)
    sedan_route_point_471: pose_3d with:
        keep(it.position.x == -165.728m)
        keep(it.position.y == 153.271m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.53613rad)
    sedan_route_point_472: pose_3d with:
        keep(it.position.x == -165.979m)
        keep(it.position.y == 153.453m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.49017rad)
    sedan_route_point_473: pose_3d with:
        keep(it.position.x == -166.22m)
        keep(it.position.y == 153.647m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.4542rad)
    sedan_route_point_474: pose_3d with:
        keep(it.position.x == -166.529m)
        keep(it.position.y == 153.905m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_475: pose_3d with:
        keep(it.position.x == -166.778m)
        keep(it.position.y == 154.114m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_476: pose_3d with:
        keep(it.position.x == -172.953m)
        keep(it.position.y == 159.287m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_477: pose_3d with:
        keep(it.position.x == -179.128m)
        keep(it.position.y == 164.46m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_478: pose_3d with:
        keep(it.position.x == -185.303m)
        keep(it.position.y == 169.634m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_479: pose_3d with:
        keep(it.position.x == -191.478m)
        keep(it.position.y == 174.807m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_480: pose_3d with:
        keep(it.position.x == -197.653m)
        keep(it.position.y == 179.981m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_481: pose_3d with:
        keep(it.position.x == -203.828m)
        keep(it.position.y == 185.154m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_482: pose_3d with:
        keep(it.position.x == -210.003m)
        keep(it.position.y == 190.327m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44422rad)
    sedan_route_point_483: pose_3d with:
        keep(it.position.x == -216.178m)
        keep(it.position.y == 195.501m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44438rad)
    sedan_route_point_484: pose_3d with:
        keep(it.position.x == -217.853m)
        keep(it.position.y == 196.902m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.44779rad)
    sedan_route_point_485: pose_3d with:
        keep(it.position.x == -220.38m)
        keep(it.position.y == 198.996m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.45295rad)
    sedan_route_point_486: pose_3d with:
        keep(it.position.x == -222.921m)
        keep(it.position.y == 201.074m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.4581rad)
    sedan_route_point_487: pose_3d with:
        keep(it.position.x == -224.619m)
        keep(it.position.y == 202.448m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.46147rad)
    sedan_route_point_488: pose_3d with:
        keep(it.position.x == -229.127m)
        keep(it.position.y == 206.094m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.46167rad)
    sedan_route_point_489: pose_3d with:
        keep(it.position.x == -233.636m)
        keep(it.position.y == 209.739m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.46167rad)
    sedan_route_point_490: pose_3d with:
        keep(it.position.x == -238.145m)
        keep(it.position.y == 213.385m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.46167rad)
    sedan_route_point_491: pose_3d with:
        keep(it.position.x == -242.654m)
        keep(it.position.y == 217.03m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.46069rad)
    sedan_route_point_492: pose_3d with:
        keep(it.position.x == -242.882m)
        keep(it.position.y == 217.222m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.42153rad)
    sedan_route_point_493: pose_3d with:
        keep(it.position.x == -243.098m)
        keep(it.position.y == 217.42m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.38069rad)
    sedan_route_point_494: pose_3d with:
        keep(it.position.x == -243.307m)
        keep(it.position.y == 217.627m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.34021rad)
    sedan_route_point_495: pose_3d with:
        keep(it.position.x == -243.506m)
        keep(it.position.y == 217.842m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.29972rad)
    sedan_route_point_496: pose_3d with:
        keep(it.position.x == -243.697m)
        keep(it.position.y == 218.064m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.25923rad)
    sedan_route_point_497: pose_3d with:
        keep(it.position.x == -243.879m)
        keep(it.position.y == 218.295m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.21874rad)
    sedan_route_point_498: pose_3d with:
        keep(it.position.x == -244.051m)
        keep(it.position.y == 218.532m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.17826rad)
    sedan_route_point_499: pose_3d with:
        keep(it.position.x == -244.214m)
        keep(it.position.y == 218.776m)
        keep(it.position.z == 0.15239m)
        keep(it.orientation.yaw == 2.12853rad)
    sedan_route_point_500: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.151385m)
        keep(it.orientation.yaw == 2.10829rad)
    sedan_route_point_501: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.15013m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_502: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.149314m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_503: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148859m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_504: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.14862m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_505: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148497m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_506: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148435m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_507: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148404m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_508: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148388m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_509: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148381m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_510: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148377m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_511: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148375m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_512: pose_3d with:
        keep(it.position.x == -244.433m)
        keep(it.position.y == 219.143m)
        keep(it.position.z == 0.148373m)
        keep(it.orientation.yaw == 2.04933rad)
    sedan_route_point_513: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.148373m)
        keep(it.orientation.yaw == 2.04933rad)
    sedan_route_point_514: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.146113m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_515: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.144796m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_516: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.144089m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_517: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143724m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_518: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143539m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_519: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143446m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_520: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143399m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_521: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143375m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_522: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143363m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_523: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143357m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_524: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143355m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_525: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143353m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_526: pose_3d with:
        keep(it.position.x == -244.629m)
        keep(it.position.y == 219.522m)
        keep(it.position.z == 0.143352m)
        keep(it.orientation.yaw == 1.99037rad)
    sedan_route_point_527: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.143352m)
        keep(it.orientation.yaw == 1.99037rad)
    sedan_route_point_528: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.140088m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_529: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.138268m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_530: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.137311m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_531: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136821m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_532: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136572m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_533: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136447m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_534: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136385m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_535: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136354m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_536: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136338m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_537: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.13633m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_538: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136326m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_539: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136324m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_540: pose_3d with:
        keep(it.position.x == -244.803m)
        keep(it.position.y == 219.912m)
        keep(it.position.z == 0.136323m)
        keep(it.orientation.yaw == 1.93141rad)
    sedan_route_point_541: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.136322m)
        keep(it.orientation.yaw == 1.93141rad)
    sedan_route_point_542: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.132054m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_543: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.129733m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_544: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.128524m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_545: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127908m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_546: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127597m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_547: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127441m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_548: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127363m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_549: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127323m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_550: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127304m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_551: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127294m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_552: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127289m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_553: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127287m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_554: pose_3d with:
        keep(it.position.x == -244.954m)
        keep(it.position.y == 220.312m)
        keep(it.position.z == 0.127285m)
        keep(it.orientation.yaw == 1.87245rad)
    sedan_route_point_555: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.127284m)
        keep(it.orientation.yaw == 1.87245rad)
    sedan_route_point_556: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.122012m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_557: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.119188m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_558: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.117729m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_559: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116987m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_560: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116614m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_561: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116426m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_562: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116332m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_563: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116285m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_564: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116261m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_565: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.11625m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_566: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116244m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_567: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116241m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_568: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116239m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_569: pose_3d with:
        keep(it.position.x == -245.081m)
        keep(it.position.y == 220.72m)
        keep(it.position.z == 0.116238m)
        keep(it.orientation.yaw == 1.81349rad)
    sedan_route_point_570: pose_3d with:
        keep(it.position.x == -245.184m)
        keep(it.position.y == 221.134m)
        keep(it.position.z == 0.116238m)
        keep(it.orientation.yaw == 1.78402rad)
    sedan_route_point_571: pose_3d with:
        keep(it.position.x == -245.262m)
        keep(it.position.y == 221.554m)
        keep(it.position.z == 0.103183m)
        keep(it.orientation.yaw == 1.72506rad)
    sedan_route_point_572: pose_3d with:
        keep(it.position.x == -245.315m)
        keep(it.position.y == 221.978m)
        keep(it.position.z == 0.0881199m)
        keep(it.orientation.yaw == 1.68562rad)
    sedan_route_point_573: pose_3d with:
        keep(it.position.x == -245.327m)
        keep(it.position.y == 222.122m)
        keep(it.position.z == 0.082696m)
        keep(it.orientation.yaw == 1.6442rad)
    sedan_route_point_574: pose_3d with:
        keep(it.position.x == -245.338m)
        keep(it.position.y == 222.294m)
        keep(it.position.z == 0.0763146m)
        keep(it.orientation.yaw == 1.59781rad)
    sedan_route_point_575: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0584283m)
        keep(it.orientation.yaw == 1.58587rad)
    sedan_route_point_576: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0503793m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_577: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0465798m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_578: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0447352m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_579: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0438266m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_580: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0433756m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_581: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.043151m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_582: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0430389m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_583: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429829m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_584: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429549m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_585: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429409m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_586: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429339m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_587: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429304m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_588: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429287m)
        keep(it.orientation.yaw == 0rad)
    sedan_route_point_589: pose_3d with:
        keep(it.position.x == -245.346m)
        keep(it.position.y == 222.822m)
        keep(it.position.z == 0.0429274m)
        keep(it.orientation.yaw == 1.51299rad)
    sedan_route_point_590: pose_3d with:
        keep(it.position.x == -245.315m)
        keep(it.position.y == 223.349m)
        keep(it.position.z == 0.042927m)
        keep(it.orientation.yaw == 1.47655rad)
    sedan_route_point_591: pose_3d with:
        keep(it.position.x == -245.247m)
        keep(it.position.y == 223.873m)
        keep(it.position.z == 0.0298104m)
        keep(it.orientation.yaw == 1.40368rad)
    sedan_route_point_592: pose_3d with:
        keep(it.position.x == -245.14m)
        keep(it.position.y == 224.39m)
        keep(it.position.z == 0.0190786m)
        keep(it.orientation.yaw == 1.3308rad)
    sedan_route_point_593: pose_3d with:
        keep(it.position.x == -244.996m)
        keep(it.position.y == 224.898m)
        keep(it.position.z == 0.0107317m)
        keep(it.orientation.yaw == 1.25792rad)
    sedan_route_point_594: pose_3d with:
        keep(it.position.x == -244.815m)
        keep(it.position.y == 225.394m)
        keep(it.position.z == 0.00476966m)
        keep(it.orientation.yaw == 1.18505rad)
    sedan_route_point_595: pose_3d with:
        keep(it.position.x == -244.599m)
        keep(it.position.y == 225.875m)
        keep(it.position.z == 0.00119242m)
        keep(it.orientation.yaw == 1.11217rad)
    sedan_route_point_596: pose_3d with:
        keep(it.position.x == -244.348m)
        keep(it.position.y == 226.34m)
        keep(it.orientation.yaw == 1.04369rad)
    sedan_route_point_597: pose_3d with:
        keep(it.position.x == -244.1m)
        keep(it.position.y == 226.732m)
        keep(it.orientation.yaw == 0.975206rad)
    sedan_route_point_598: pose_3d with:
        keep(it.position.x == -243.827m)
        keep(it.position.y == 227.108m)
        keep(it.orientation.yaw == 0.911121rad)
    sedan_route_point_599: pose_3d with:
        keep(it.position.x == -243.531m)
        keep(it.position.y == 227.465m)
        keep(it.orientation.yaw == 0.847035rad)
    sedan_route_point_600: pose_3d with:
        keep(it.position.x == -243.213m)
        keep(it.position.y == 227.803m)
        keep(it.orientation.yaw == 0.784874rad)
    sedan_route_point_601: pose_3d with:
        keep(it.position.x == -238.064m)
        keep(it.position.y == 232.927m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_point_602: pose_3d with:
        keep(it.position.x == -232.918m)
        keep(it.position.y == 238.048m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_point_603: pose_3d with:
        keep(it.position.x == -227.773m)
        keep(it.position.y == 243.168m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_point_604: pose_3d with:
        keep(it.position.x == -222.627m)
        keep(it.position.y == 248.288m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_point_605: pose_3d with:
        keep(it.position.x == -217.482m)
        keep(it.position.y == 253.409m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_point_606: pose_3d with:
        keep(it.position.x == -212.616m)
        keep(it.position.y == 258.251m)
        keep(it.orientation.yaw == 0.78295rad)
    sedan_route_points: list of pose_3d = [sedan_route_point_1, sedan_route_point_2, sedan_route_point_3, sedan_route_point_4, sedan_route_point_5, sedan_route_point_6, sedan_route_point_7, sedan_route_point_8, sedan_route_point_9, sedan_route_point_10, sedan_route_point_11, sedan_route_point_12, sedan_route_point_13, sedan_route_point_14, sedan_route_point_15, sedan_route_point_16, sedan_route_point_17, sedan_route_point_18, sedan_route_point_19, sedan_route_point_20, sedan_route_point_21, sedan_route_point_22, sedan_route_point_23, sedan_route_point_24, sedan_route_point_25, sedan_route_point_26, sedan_route_point_27, sedan_route_point_28, sedan_route_point_29, sedan_route_point_30, sedan_route_point_31, sedan_route_point_32, sedan_route_point_33, sedan_route_point_34, sedan_route_point_35, sedan_route_point_36, sedan_route_point_37, sedan_route_point_38, sedan_route_point_39, sedan_route_point_40, sedan_route_point_41, sedan_route_point_42, sedan_route_point_43, sedan_route_point_44, sedan_route_point_45, sedan_route_point_46, sedan_route_point_47, sedan_route_point_48, sedan_route_point_49, sedan_route_point_50, sedan_route_point_51, sedan_route_point_52, sedan_route_point_53, sedan_route_point_54, sedan_route_point_55, sedan_route_point_56, sedan_route_point_57, sedan_route_point_58, sedan_route_point_59, sedan_route_point_60, sedan_route_point_61, sedan_route_point_62, sedan_route_point_63, sedan_route_point_64, sedan_route_point_65, sedan_route_point_66, sedan_route_point_67, sedan_route_point_68, sedan_route_point_69, sedan_route_point_70, sedan_route_point_71, sedan_route_point_72, sedan_route_point_73, sedan_route_point_74, sedan_route_point_75, sedan_route_point_76, sedan_route_point_77, sedan_route_point_78, sedan_route_point_79, sedan_route_point_80, sedan_route_point_81, sedan_route_point_82, sedan_route_point_83, sedan_route_point_84, sedan_route_point_85, sedan_route_point_86, sedan_route_point_87, sedan_route_point_88, sedan_route_point_89, sedan_route_point_90, sedan_route_point_91, sedan_route_point_92, sedan_route_point_93, sedan_route_point_94, sedan_route_point_95, sedan_route_point_96, sedan_route_point_97, sedan_route_point_98, sedan_route_point_99, sedan_route_point_100, sedan_route_point_101, sedan_route_point_102, sedan_route_point_103, sedan_route_point_104, sedan_route_point_105, sedan_route_point_106, sedan_route_point_107, sedan_route_point_108, sedan_route_point_109, sedan_route_point_110, sedan_route_point_111, sedan_route_point_112, sedan_route_point_113, sedan_route_point_114, sedan_route_point_115, sedan_route_point_116, sedan_route_point_117, sedan_route_point_118, sedan_route_point_119, sedan_route_point_120, sedan_route_point_121, sedan_route_point_122, sedan_route_point_123, sedan_route_point_124, sedan_route_point_125, sedan_route_point_126, sedan_route_point_127, sedan_route_point_128, sedan_route_point_129, sedan_route_point_130, sedan_route_point_131, sedan_route_point_132, sedan_route_point_133, sedan_route_point_134, sedan_route_point_135, sedan_route_point_136, sedan_route_point_137, sedan_route_point_138, sedan_route_point_139, sedan_route_point_140, sedan_route_point_141, sedan_route_point_142, sedan_route_point_143, sedan_route_point_144, sedan_route_point_145, sedan_route_point_146, sedan_route_point_147, sedan_route_point_148, sedan_route_point_149, sedan_route_point_150, sedan_route_point_151, sedan_route_point_152, sedan_route_point_153, sedan_route_point_154, sedan_route_point_155, sedan_route_point_156, sedan_route_point_157, sedan_route_point_158, sedan_route_point_159, sedan_route_point_160, sedan_route_point_161, sedan_route_point_162, sedan_route_point_163, sedan_route_point_164, sedan_route_point_165, sedan_route_point_166, sedan_route_point_167, sedan_route_point_168, sedan_route_point_169, sedan_route_point_170, sedan_route_point_171, sedan_route_point_172, sedan_route_point_173, sedan_route_point_174, sedan_route_point_175, sedan_route_point_176, sedan_route_point_177, sedan_route_point_178, sedan_route_point_179, sedan_route_point_180, sedan_route_point_181, sedan_route_point_182, sedan_route_point_183, sedan_route_point_184, sedan_route_point_185, sedan_route_point_186, sedan_route_point_187, sedan_route_point_188, sedan_route_point_189, sedan_route_point_190, sedan_route_point_191, sedan_route_point_192, sedan_route_point_193, sedan_route_point_194, sedan_route_point_195, sedan_route_point_196, sedan_route_point_197, sedan_route_point_198, sedan_route_point_199, sedan_route_point_200, sedan_route_point_201, sedan_route_point_202, sedan_route_point_203, sedan_route_point_204, sedan_route_point_205, sedan_route_point_206, sedan_route_point_207, sedan_route_point_208, sedan_route_point_209, sedan_route_point_210, sedan_route_point_211, sedan_route_point_212, sedan_route_point_213, sedan_route_point_214, sedan_route_point_215, sedan_route_point_216, sedan_route_point_217, sedan_route_point_218, sedan_route_point_219, sedan_route_point_220, sedan_route_point_221, sedan_route_point_222, sedan_route_point_223, sedan_route_point_224, sedan_route_point_225, sedan_route_point_226, sedan_route_point_227, sedan_route_point_228, sedan_route_point_229, sedan_route_point_230, sedan_route_point_231, sedan_route_point_232, sedan_route_point_233, sedan_route_point_234, sedan_route_point_235, sedan_route_point_236, sedan_route_point_237, sedan_route_point_238, sedan_route_point_239, sedan_route_point_240, sedan_route_point_241, sedan_route_point_242, sedan_route_point_243, sedan_route_point_244, sedan_route_point_245, sedan_route_point_246, sedan_route_point_247, sedan_route_point_248, sedan_route_point_249, sedan_route_point_250, sedan_route_point_251, sedan_route_point_252, sedan_route_point_253, sedan_route_point_254, sedan_route_point_255, sedan_route_point_256, sedan_route_point_257, sedan_route_point_258, sedan_route_point_259, sedan_route_point_260, sedan_route_point_261, sedan_route_point_262, sedan_route_point_263, sedan_route_point_264, sedan_route_point_265, sedan_route_point_266, sedan_route_point_267, sedan_route_point_268, sedan_route_point_269, sedan_route_point_270, sedan_route_point_271, sedan_route_point_272, sedan_route_point_273, sedan_route_point_274, sedan_route_point_275, sedan_route_point_276, sedan_route_point_277, sedan_route_point_278, sedan_route_point_279, sedan_route_point_280, sedan_route_point_281, sedan_route_point_282, sedan_route_point_283, sedan_route_point_284, sedan_route_point_285, sedan_route_point_286, sedan_route_point_287, sedan_route_point_288, sedan_route_point_289, sedan_route_point_290, sedan_route_point_291, sedan_route_point_292, sedan_route_point_293, sedan_route_point_294, sedan_route_point_295, sedan_route_point_296, sedan_route_point_297, sedan_route_point_298, sedan_route_point_299, sedan_route_point_300, sedan_route_point_301, sedan_route_point_302, sedan_route_point_303, sedan_route_point_304, sedan_route_point_305, sedan_route_point_306, sedan_route_point_307, sedan_route_point_308, sedan_route_point_309, sedan_route_point_310, sedan_route_point_311, sedan_route_point_312, sedan_route_point_313, sedan_route_point_314, sedan_route_point_315, sedan_route_point_316, sedan_route_point_317, sedan_route_point_318, sedan_route_point_319, sedan_route_point_320, sedan_route_point_321, sedan_route_point_322, sedan_route_point_323, sedan_route_point_324, sedan_route_point_325, sedan_route_point_326, sedan_route_point_327, sedan_route_point_328, sedan_route_point_329, sedan_route_point_330, sedan_route_point_331, sedan_route_point_332, sedan_route_point_333, sedan_route_point_334, sedan_route_point_335, sedan_route_point_336, sedan_route_point_337, sedan_route_point_338, sedan_route_point_339, sedan_route_point_340, sedan_route_point_341, sedan_route_point_342, sedan_route_point_343, sedan_route_point_344, sedan_route_point_345, sedan_route_point_346, sedan_route_point_347, sedan_route_point_348, sedan_route_point_349, sedan_route_point_350, sedan_route_point_351, sedan_route_point_352, sedan_route_point_353, sedan_route_point_354, sedan_route_point_355, sedan_route_point_356, sedan_route_point_357, sedan_route_point_358, sedan_route_point_359, sedan_route_point_360, sedan_route_point_361, sedan_route_point_362, sedan_route_point_363, sedan_route_point_364, sedan_route_point_365, sedan_route_point_366, sedan_route_point_367, sedan_route_point_368, sedan_route_point_369, sedan_route_point_370, sedan_route_point_371, sedan_route_point_372, sedan_route_point_373, sedan_route_point_374, sedan_route_point_375, sedan_route_point_376, sedan_route_point_377, sedan_route_point_378, sedan_route_point_379, sedan_route_point_380, sedan_route_point_381, sedan_route_point_382, sedan_route_point_383, sedan_route_point_384, sedan_route_point_385, sedan_route_point_386, sedan_route_point_387, sedan_route_point_388, sedan_route_point_389, sedan_route_point_390, sedan_route_point_391, sedan_route_point_392, sedan_route_point_393, sedan_route_point_394, sedan_route_point_395, sedan_route_point_396, sedan_route_point_397, sedan_route_point_398, sedan_route_point_399, sedan_route_point_400, sedan_route_point_401, sedan_route_point_402, sedan_route_point_403, sedan_route_point_404, sedan_route_point_405, sedan_route_point_406, sedan_route_point_407, sedan_route_point_408, sedan_route_point_409, sedan_route_point_410, sedan_route_point_411, sedan_route_point_412, sedan_route_point_413, sedan_route_point_414, sedan_route_point_415, sedan_route_point_416, sedan_route_point_417, sedan_route_point_418, sedan_route_point_419, sedan_route_point_420, sedan_route_point_421, sedan_route_point_422, sedan_route_point_423, sedan_route_point_424, sedan_route_point_425, sedan_route_point_426, sedan_route_point_427, sedan_route_point_428, sedan_route_point_429, sedan_route_point_430, sedan_route_point_431, sedan_route_point_432, sedan_route_point_433, sedan_route_point_434, sedan_route_point_435, sedan_route_point_436, sedan_route_point_437, sedan_route_point_438, sedan_route_point_439, sedan_route_point_440, sedan_route_point_441, sedan_route_point_442, sedan_route_point_443, sedan_route_point_444, sedan_route_point_445, sedan_route_point_446, sedan_route_point_447, sedan_route_point_448, sedan_route_point_449, sedan_route_point_450, sedan_route_point_451, sedan_route_point_452, sedan_route_point_453, sedan_route_point_454, sedan_route_point_455, sedan_route_point_456, sedan_route_point_457, sedan_route_point_458, sedan_route_point_459, sedan_route_point_460, sedan_route_point_461, sedan_route_point_462, sedan_route_point_463, sedan_route_point_464, sedan_route_point_465, sedan_route_point_466, sedan_route_point_467, sedan_route_point_468, sedan_route_point_469, sedan_route_point_470, sedan_route_point_471, sedan_route_point_472, sedan_route_point_473, sedan_route_point_474, sedan_route_point_475, sedan_route_point_476, sedan_route_point_477, sedan_route_point_478, sedan_route_point_479, sedan_route_point_480, sedan_route_point_481, sedan_route_point_482, sedan_route_point_483, sedan_route_point_484, sedan_route_point_485, sedan_route_point_486, sedan_route_point_487, sedan_route_point_488, sedan_route_point_489, sedan_route_point_490, sedan_route_point_491, sedan_route_point_492, sedan_route_point_493, sedan_route_point_494, sedan_route_point_495, sedan_route_point_496, sedan_route_point_497, sedan_route_point_498, sedan_route_point_499, sedan_route_point_500, sedan_route_point_501, sedan_route_point_502, sedan_route_point_503, sedan_route_point_504, sedan_route_point_505, sedan_route_point_506, sedan_route_point_507, sedan_route_point_508, sedan_route_point_509, sedan_route_point_510, sedan_route_point_511, sedan_route_point_512, sedan_route_point_513, sedan_route_point_514, sedan_route_point_515, sedan_route_point_516, sedan_route_point_517, sedan_route_point_518, sedan_route_point_519, sedan_route_point_520, sedan_route_point_521, sedan_route_point_522, sedan_route_point_523, sedan_route_point_524, sedan_route_point_525, sedan_route_point_526, sedan_route_point_527, sedan_route_point_528, sedan_route_point_529, sedan_route_point_530, sedan_route_point_531, sedan_route_point_532, sedan_route_point_533, sedan_route_point_534, sedan_route_point_535, sedan_route_point_536, sedan_route_point_537, sedan_route_point_538, sedan_route_point_539, sedan_route_point_540, sedan_route_point_541, sedan_route_point_542, sedan_route_point_543, sedan_route_point_544, sedan_route_point_545, sedan_route_point_546, sedan_route_point_547, sedan_route_point_548, sedan_route_point_549, sedan_route_point_550, sedan_route_point_551, sedan_route_point_552, sedan_route_point_553, sedan_route_point_554, sedan_route_point_555, sedan_route_point_556, sedan_route_point_557, sedan_route_point_558, sedan_route_point_559, sedan_route_point_560, sedan_route_point_561, sedan_route_point_562, sedan_route_point_563, sedan_route_point_564, sedan_route_point_565, sedan_route_point_566, sedan_route_point_567, sedan_route_point_568, sedan_route_point_569, sedan_route_point_570, sedan_route_point_571, sedan_route_point_572, sedan_route_point_573, sedan_route_point_574, sedan_route_point_575, sedan_route_point_576, sedan_route_point_577, sedan_route_point_578, sedan_route_point_579, sedan_route_point_580, sedan_route_point_581, sedan_route_point_582, sedan_route_point_583, sedan_route_point_584, sedan_route_point_585, sedan_route_point_586, sedan_route_point_587, sedan_route_point_588, sedan_route_point_589, sedan_route_point_590, sedan_route_point_591, sedan_route_point_592, sedan_route_point_593, sedan_route_point_594, sedan_route_point_595, sedan_route_point_596, sedan_route_point_597, sedan_route_point_598, sedan_route_point_599, sedan_route_point_600, sedan_route_point_601, sedan_route_point_602, sedan_route_point_603, sedan_route_point_604, sedan_route_point_605, sedan_route_point_606]
    sedan_route: path = map.create_path(points: sedan_route_points, interpolation: smooth)

    point_607: position_3d with:
        keep(it.x == -77.4998m)
        keep(it.y == 84.1461m)

    # Event declaration to express scenario failure condition 
    event mw_scenario_fail_event is false

    on @mw_scenario_fail_event:
        emit fail(22_test_scenario)

    do root_phase: parallel:
        sedan.drive() with:
            along(sedan_route)
            physical_movement(prefer_non_physical)
        phase_1: parallel:
            phase_2: serial:
                phase_3: sedan.assign_speed(0mps)
                phase_4: sedan.change_speed(13.9mps, rate_profile: constant, rate_peak: 4mpss) with:
                    until (sedan.mw_point_euclidean_dist(point_607) <= 0m)
                phase_5: sedan.change_speed(8mps, rate_profile: constant, rate_peak: 4mpss)
        with:
            until (environment.datetime - sim_start_time >= 60s)

